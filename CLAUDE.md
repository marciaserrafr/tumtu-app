# CLAUDE.md

> Este arquivo é lido automaticamente pelo Claude Code toda vez que uma sessão começa nesta pasta. É a ficha de instruções fixas do projeto — diferente dos documentos em `docs/`, que são a documentação de produto/técnica para humanos. Atualizar sempre que uma decisão importante mudar o que está escrito aqui.
> Última atualização: 27/ago/2026

---

## O que é o TumTu

SaaS de gestão de baterias de escola de samba: cadastro de ritmistas, aprovação por Mestre/Diretor, carteirinha digital. Nome vem do som do surdo de bateria ("TUM-TU, TUM-TU..."). Visão de virar produto comercial multi-escola no futuro.

**Quem constrói:** Márcia Serra — empreendedora, **não é desenvolvedora**, zero conhecimento prévio de programação. Todo o código é escrito pela IA (Claude Code); Márcia decide produto, UX e regras de negócio. Ela está aprendendo sobre IA construindo este projeto ao mesmo tempo.

---

## Antes de mexer em qualquer coisa: mapa da documentação

Este projeto tem documentação de produto detalhada em `docs/` — **leia o documento relevante antes de qualquer mudança estrutural**, não assuma.

| Arquivo | Cobre | Quando ler |
|---|---|---|
| `docs/tumtu-visao-geral.md` | Visão de negócio, marca, identidade visual, jurídico/LGPD | Antes de decisões de produto/negócio |
| `docs/tumtu-mvp.md` | Escopo funcional do MVP, perfis, fluxos, regras numeradas (1 a 11) | Antes de mudar fluxo de cadastro/aprovação/permissão |
| `docs/tumtu-design-guide.md` | Paleta, tipografia, componentes visuais, checklist obrigatório antes de mudança visual | **Sempre antes de qualquer alteração visual** |
| `docs/tumtu-documentacao-tecnica.md` | Arquitetura, modelo de dados, RLS, Edge Function, histórico de decisões técnicas | Antes de mexer em banco, autenticação, permissões |
| `docs/tumtu-plano-de-testes.md` | Estratégia de teste, dados fake, roteiros de teste manual | Antes de testar mudanças ou popular dados |
| `docs/tumtu-estrategia-piloto.md` | Estratégia de entrada via carteirinha beta gratuita, operação do piloto (problema de ritmista em múltiplas baterias já resolvido em 13/jul/2026, ver seção 14) | Antes de mexer em cadastro/aprovação/login pensando no piloto |

`docs/README.md` **não existe mais** — foi removido em 09/jul/2026 por descrever uma versão bem antiga do sistema (senha em texto puro, sem Super Admin) que divergia dos 4 docs acima.

---

## Regras que eu nunca posso quebrar

### Comportamento geral
- **Nunca mudar layout, espaçamento ou organização visual sem aprovação explícita.** Se a mudança não foi pedida, não faço. Quando há dúvida sobre estilo, nome de botão, cor ou layout, mostro opções e espero a Márcia escolher antes de implementar.
- **Nunca remover/desistir de algo que ela pediu, mesmo depois de várias tentativas sem sucesso, sem perguntar antes.** Regra criada em 22/ago/2026, depois de eu remover sozinho o rótulo "Nome usado" (Configurações → Instrumentos) porque não conseguia acertar o alinhamento dele — ela tinha pedido pra AJUSTAR, não pra tirar, e ficou justificadamente brava ("QUEM MANDOU VOCÊ TIRAR?"). Se depois de várias tentativas eu não estiver conseguindo acertar algo visual, o caminho certo é falar isso pra ela e perguntar como seguir (tentar de outro jeito, pausar, ou só aí sim remover) — nunca decidir sozinho que "o mais simples é tirar" e simplesmente fazer.
- **Nunca adicionar detalhe visual não pedido** (separadores, ícones extras, cores, espaçamentos) — implementar exatamente o que foi descrito, nada a mais.
- **Confirmar antes de subir ao GitHub.** Regra reforçada em 18/jul/2026 (a Márcia pediu essa "rede de segurança" explicitamente, depois de perceber que hoje toda mudança vai direto pra produção sem ambiente de teste separado): antes de publicar de verdade (`git push` na `main`, que a Vercel publica automaticamente em `tumtu.com.br`), gerar um **link de teste avulso** e mandar pra ela conferir no próprio celular/computador — não só descrever em palavras ou mostrar print estático. Só depois da aprovação dela, commit + push de verdade (ou merge do branch de preview pra `main`). Vale pra qualquer mudança visual ou de fluxo perceptível por quem usa o app — não precisa pra ajustes 100% internos (ex: só comentário de código, só reorganização de memória). **Método atualizado em 14/ago/2026** (o `vercel` CLI não está disponível neste ambiente — ver "Stack e infraestrutura"): commitar num branch dedicado (`preview/nome-da-mudanca`, nunca direto na `main`) → `git push` desse branch → a integração GitHub↔Vercel do projeto `tumtu-app` publica sozinha um preview pra esse branch, sem comando manual → pegar a URL estável desse preview (formato `tumtu-app-git-<branch>-marcia-ritmistas.vercel.app`, via ferramenta MCP `list_deployments`) → gerar um link sem exigência de login da Vercel com a ferramenta MCP `get_access_to_vercel_url` (expira em ~23h) e mandar pra ela abrir **direto no navegador** (não de dentro do WhatsApp/Instagram — o navegador embutido desses apps pode bloquear o cookie que esse link depende, já aconteceu de "funcionar" do lado de cá e não abrir pra ela). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 32.
- **Quando a Márcia disser "estava melhor antes"** — reverter imediatamente, sem questionar e sem tentar "melhorar" a versão anterior por conta própria.
- **Dimensão da carteirinha é fixa em 300×540px (documentado em `docs/tumtu-design-guide.md`, seção 7.3) — nunca aumentar o tamanho do cartão pra caber conteúdo novo.** Se fonte, logo, respiro ou qualquer elemento não couber, o ajuste é no conteúdo (reduzir/redistribuir), nunca no tamanho do cartão. Mudar essa dimensão exige aprovação explícita da Márcia todas as vezes — regra criada em 13/jul/2026 depois de eu aumentar o tamanho sem avisar duas vezes seguidas na mesma sessão. (Único ajuste aprovado desde então: 500px→540px de altura em 14/jul/2026, a pedido explícito dela — "um pouquinho mais comprida"; a regra de nunca mudar sem aprovar de novo continua valendo.)

### Regras de produto (não violar sem confirmar com a Márcia)
- **Um único formulário de cadastro** (`cadastro.html`, renomeado de `index.html` em 12/jul/2026 — ver seção "Domínio" abaixo), reaproveitado em 3 modos (público/link fixo/manual) — nunca duplicar em telas separadas. O modo "público" (escolher escola livremente) existe no código mas não é linkado de lugar nenhum hoje — todo cadastro real acontece via link fixo, vinculado a uma bateria específica.
- **Um único motor de edição de perfil** (`ficha-perfil.js` + `ficha-perfil.partial.html`) compartilhado por `admin.html` e `carteirinha.html` — nunca copiar/colar essa lógica numa tela nova. (`super-admin.html` foi apagado em 19/ago/2026 — `admin.html` unificou as duas telas, ver "Estado atual".)
- **Telas que mostram o mesmo modelo de dados para perfis diferentes devem ter os mesmos campos.** Ex: modal de Admin e modal de Ritmista mostram os mesmos campos — a diferença entre perfis é sobre quem pode ver/editar, não sobre quais campos existem na tela.
- **Dados de escola são sempre variáveis via `config-escola.js`** — nunca hardcoded no código.
- **Conta de Super Admin nunca é exposta a exclusão pela interface**, em nenhuma tela — evita lockout, já que ninguém acima dela pode restaurar o acesso.
- **Super Admin nunca tem acesso a ver/definir a senha de outra pessoa** — decisão ética explícita da Márcia. Reset de senha é sempre self-service via Supabase Auth nativo.

### Priorização
- **Função antes de polimento.** Ao sugerir o que atacar a seguir, separar "não funciona / está quebrado" de "funciona mas podia ser mais conveniente/bonito" — na dúvida, priorizar o primeiro grupo. Não presumir que uma melhoria de UX bem definida é prioridade só por estar pronta pra implementar.
- **O card do ritmista é o elemento mais importante da tela do Admin.** Tudo ao redor (filtros, vagas, seções extras) é secundário — deve ficar clean, sem poluir visualmente o card.

### Hierarquia visual
- Botão de ação principal (ex: "Cadastro") precisa de destaque real: fundo sólido escuro `#12101a` ou dourado `#D4AF37` (tokens `--cor-fundo-escuro`/`--cor-destaque` em `styles/tokens.css`). Botões secundários = só borda, transparente.
- Seções auxiliares (ex: "Vagas por Instrumento") devem ser simples — listas com texto, sem chips/cards aninhados/cores excessivas.

---

## Como trabalhar com a Márcia

- Explicar tudo do zero absoluto — o que é cada ferramenta, por que está sendo usada, cada passo em linguagem simples. Nunca assumir conhecimento técnico prévio, inclusive sobre onde clicar em painéis externos (Supabase, GitHub, Vercel).
- Ela fica ansiosa com telas desconhecidas (painel de controle de versão, opções administrativas do Supabase) — tranquilizar primeiro ("nada quebrou, nada foi feito errado") antes de explicar o próximo passo.
- Ela está aos poucos aprendendo a se organizar (pastas, versionamento) — reforçar positivamente esses avanços, sem forçar demais de uma vez.

---

## Stack e infraestrutura

- **Frontend:** HTML + CSS + JavaScript puro. Sem framework, sem bundler, sem `package.json`. Cada tela é um `.html` autocontido.
- **Backend:** Supabase (Postgres + Auth + Edge Functions), sem servidor próprio. Project ref `pkvzsgrkylrkyzligeim`.
- **E-mail transacional (e-mails automáticos do Supabase Auth — redefinir senha, etc.):** servidor de envio próprio via **Resend** (custom SMTP, configurado em Authentication → Emails → SMTP Settings do Supabase, 14/ago/2026) — sem isso, o Supabase usa um servidor genérico deles e trava a edição do texto dos e-mails, que ficam em inglês. Conta do Resend é a pessoal da Márcia (`marciaserrafr@gmail.com`, mesmo padrão de GitHub/Vercel/Supabase — `tumtuapp@gmail.com` fica só pro papel de Super Admin dentro do próprio app). Domínio `tumtu.com.br` verificado no Resend via registros DNS na zona do **Registro.br** (não é a Vercel que hospeda o DNS). Remetente reaproveita o `suporte@tumtu.com.br` já existente. **Só o template "Reset password" foi traduzido até agora** — os outros e-mails do Supabase (confirmação de cadastro, etc.) continuam em inglês, sem confirmação ainda se algum deles é realmente enviado no fluxo atual do app. Detalhe completo: `tumtu-documentacao-tecnica.md` seção 33.
- **Deploy:** GitHub (`marciaserrafr/tumtu-app`) → Vercel (projeto **`tumtu-app`**, team `marcia-ritmistas` — é o projeto com a integração GitHub de verdade, publica automático a cada `git push`, tanto na `main` quanto em qualquer outro branch, como preview). **Existe um projeto Vercel antigo separado, `ritmistas-app`, que NÃO é esse** — tem vários deploys avulsos de teste sem uso, criados por engano numa sessão anterior; não usar.
- **Ferramentas de linha de comando disponíveis localmente:** `gh` (GitHub CLI, autenticado) — usar em vez de pedir pra Márcia navegar em painéis desconhecidos, sempre que a tarefa permitir (ex: `gh repo`). **O `vercel` CLI NÃO está disponível neste ambiente** (confirmado 13/ago/2026 — `command -v vercel` não encontra nada, apesar de anotação antiga aqui dizer que estaria instalado). Pra qualquer necessidade de deploy/preview/consulta à Vercel, usar as ferramentas MCP (`mcp__claude_ai_Vercel__*`: `list_deployments`, `get_access_to_vercel_url`, `get_project_deployment_protection`, etc.) — ver fluxo completo na regra "Confirmar antes de subir ao GitHub" acima.
- **Fonte:** Plus Jakarta Sans (Google Fonts).
- **Biblioteca de terceiros:** `@supabase/supabase-js@2` via CDN. (`bcryptjs` foi removido em 05/jul/2026 — não usar mais, autenticação é 100% Supabase Auth.)
- **PWA:** instalável direto do navegador (`manifest.json` + `sw.js`), sem loja/custo. **Atenção:** todo arquivo do "app shell" (listado em `APP_SHELL` dentro de `sw.js`) é servido do cache — ao alterar qualquer `.html`/`.css`/`.js` do shell, subir a versão de `CACHE_NAME` em `sw.js`, senão quem já visitou o site continua vendo a versão antiga.
- **Cache-busting por versão na URL (`?v=N`):** `styles/tokens.css`, `styles/components.css`, `carteirinha-tumtu.css`, `ficha-perfil.js`, `ficha-perfil.partial.html` e `config-escola.js` são referenciados com `?v=1` (ou mais recente) em todo HTML que os usa, e no `fetch()` dentro de `ficha-perfil.js`. **Toda vez que algum desses arquivos mudar, subir esse número em TODOS os lugares que o referenciam** (busca por `?v=N` no projeto) — isso é mais forte que só trocar o `CACHE_NAME`: derrota até cache de operadora/proxy no meio do caminho, porque a URL vira literalmente outro arquivo. Criado em 10/jul/2026 depois de um incidente real onde a Márcia ficou presa numa versão antiga mesmo depois de limpar cache do navegador — a causa era cache fora do controle do navegador dela.

## Estrutura de pastas

```
Tumtu/
├── *.html, *.css, *.js        # código do app, solto na raiz
├── styles/                    # tokens.css (CSS variables) + components.css (componentes reutilizáveis)
├── icons/                     # ícones do PWA
├── docs/                      # documentação de produto/técnica (.md)
├── dados/                     # planilha de dados fake para popular o banco de testes
├── imagens/                   # material de referência visual — fora do Git (.gitignore)
└── .claude/
```

## Modelo de dados (resumo — detalhe completo em `docs/tumtu-documentacao-tecnica.md`, seção 22)

- **Desde 13/jul/2026, "pessoa" e "vínculo com bateria" são duas tabelas separadas** — mudança grande, ler a seção 22 da documentação técnica antes de mexer em cadastro/login/aprovação/edição de perfil:
  - **`pessoas`**: quem é a pessoa, não muda entre baterias (nome, CPF, endereço, contato de emergência, foto, `super_admin` boolean, `genero` — desde 13/jul/2026, ver seção 23 da documentação técnica). IDs `bigint`, não UUID.
  - **`vinculos`**: o vínculo de uma pessoa com UMA bateria específica (`perfil`, `status`, instrumento, os 4 tamanhos de roupa, `aprovado_por`). Uma pessoa pode ter vários vínculos, um por bateria — resolve o bug antigo de uma pessoa não conseguir se cadastrar numa segunda bateria.
  - **Convenção importante:** `.id` em card/lista/URL (`carteirinha.html?id=`) significa **vínculo**, não pessoa. Só autoedição e `aprovado_por` usam `pessoa_id` de verdade.
  - A tabela antiga `ritmistas` (que juntava as duas coisas numa linha só) ainda existe no banco como rede de segurança, sem receber mais escrita. **Atualizado 17/jul/2026:** confirmado que só 2 das 28 linhas são reais (a conta da Márcia + uma conta de teste dela, ambas já migradas corretamente para `pessoas`) — as outras 26 são dado fake. Risco de apagar agora é baixíssimo; ela reagiu bem à ideia ("todos os dados são fakes, tirando o do super admin") mas **ainda não deu o "pode apagar" explícito** — perguntar de novo antes de executar, não presumir autorização.
- `cargo` (o que aparece na carteirinha) é separado de permissão de acesso. O campo antigo `vinculos.nivel_acesso` (texto, sempre `"total"`) e as tabelas `niveis_acesso`/`escola_niveis_acesso` (perfil nomeado) ficaram obsoletos em 19/ago/2026 — permissões granulares de verdade agora vivem em `vinculos.capacidades` (jsonb, uma capacidade por chave, atribuída direto por pessoa). Ver `tumtu-documentacao-tecnica.md` seção 41.
- Tabelas `escolas` e `baterias` completam o modelo. Tabela `convites` **não existe mais** (dropada 05/jul/2026). `baterias.modo_piloto` (boolean) controla se Mestre/Diretor daquela bateria caem na carteirinha (true) ou no painel admin (false) ao logar — editável em Super Admin → Escolas → aba Bateria (interruptor adicionado 17/jul/2026). **Nome na tela é "Modo Carteirinha" desde 18/jul/2026** (o nome da coluna no banco/no código continua `modo_piloto`, só o rótulo visível mudou — mesmo padrão já usado em `status = "aprovado"` aparecendo como "Ativo" na tela). Motivo da troca: deixou de ser só uma fase temporária de teste — pode virar uma opção permanente de verdade pra uma escola que só quer a carteirinha, sem o resto da gestão.
- Autenticação real via Supabase Auth (`auth.users` ligado por `pessoas.auth_user_id`) — RLS ligado em `pessoas`, `vinculos`, `escolas`, `baterias`. Restrição por coluna (quem edita o quê) é feita por trigger (`aplicar_matriz_edicao_pessoas` + `aplicar_matriz_edicao_vinculos`), não pela policy de RLS. **Funções de RLS corretas pra "minha bateria":** `meu_pessoa_id()` e `minhas_baterias_admin()` — leem de `pessoas`/`vinculos`. **Nunca usar** `meu_perfil()`/`meu_bateria_id()`/`meu_status()` (funções antigas, liam da tabela `ritmistas` congelada — causavam permissão silenciosamente quebrada pra contas criadas depois de 13/jul; corrigido em `bateria_instrumentos` e `baterias` em 17/jul/2026, ver `tumtu-documentacao-tecnica.md` seção 27).
- Instrumentos são configuráveis (biblioteca mestre de categorias/nomenclaturas + ativação por bateria), não uma lista fixa no código — tabelas `instrumento_categorias`, `instrumento_nomenclaturas`, `bateria_instrumentos`.
- **Medidas (camisa/fantasia/calça/sapato) também são configuráveis, mesmo padrão de Instrumentos (17/jul/2026)** — biblioteca mestre em `medida_tamanhos` (tipo fixo no código, nome do tamanho configurável) + ativação por bateria em `bateria_medidas`. Dado semente: PP-XGG (camisa/fantasia/calça), 33-48 (sapato) — padrão brasileiro pesquisado. Sem "Momo" pré-cadastrado de propósito (a Márcia cadastra quando uma escola pedir).
- Vocabulário: o valor "ativo" no banco é literalmente `status = "aprovado"`, não `"ativo"`.

## Estado atual (alto nível — ver `docs/tumtu-documentacao-tecnica.md` seção 21 para histórico completo)

✅ Concluído: rename de marca Tutti→TumTu (inclusive nomes de arquivo da documentação e do repositório GitHub), autenticação real + RLS, motor único de edição de perfil, "esqueci minha senha" self-service, PWA, exportação de ritmistas para Excel (seção 17), auditoria completa de UX + cadastro em etapas (seção 19), cache-busting por versão pra evitar qualquer tipo de cache travar atualização (seção 20), configuração de instrumentos (biblioteca mestre + por bateria), domínio `tumtu.com.br` no ar (DNS + certificado SSL configurados em 12/jul/2026), migração de arquitetura "pessoa" separada de "vínculo com bateria" (seção 22 — resolve o bug de ritmista em mais de uma bateria).
✅ `index.html` agora é uma landing page provisória ("Em breve", sem nenhum link/ação) — o formulário de cadastro real foi renomeado para `cadastro.html`. Decisão de 12/jul/2026: como o domínio virou público, não faz sentido expor o cadastro aberto (modo "público", sem bateria vinculada) na porta de entrada do site.
✅ Depois da migração pessoa/vínculo: dois ajustes finos no cadastro (13/jul/2026) — "tamanhos" virou "medidas" no aviso de pessoa já existente (consistente com o nome da seção do formulário), e correção de rolagem automática (tanto a mensagem final de sucesso/erro quanto o aviso "Que bom te ver de novo" agora trazem a tela até eles, em vez de aparecerem fora da área visível em telas de computador). Tudo testado e **já enviado ao GitHub** (commits `86564c6` e `9a85130`).
✅ **Campo de gênero + nome liberado pra autoedição (13/jul/2026)** — `pessoas.genero`/`genero_personalizado` novos (Masculino/Feminino/"Prefiro me identificar como..."/Prefiro não informar, sempre opcional); `nome` saiu da trava de Super Admin, pessoa edita o próprio. Rótulo Mestre/Mestra e Diretor/Diretora já aplicado em `carteirinha.html`, `admin.html` e `login.html`. Detalhe completo: `tumtu-documentacao-tecnica.md` seção 23. Testado de ponta a ponta e **enviado ao GitHub**.
✅ **Redesign visual + arquitetura de dados da carteirinha, publicado em produção (14–16/jul/2026)** — sistema de cores dinâmico por N cores da escola (`color-mix()` a partir de até 4 cores reais em `escolas.cor_primaria/secundaria/terciaria/quaternaria`), logo da escola (frente, 96px) e da bateria (verso, 88px) via upload de arquivo de verdade (base64, mesmo padrão da foto do ritmista — `super-admin.html`), dimensão base fixa 300×540px (nunca mudar sem aprovação — ver regra abaixo). Cor/logo/nome da escola ficam em cache em `localStorage` (mesma chave usada por `login.html` e `carteirinha.html`) e são buscados **ainda na tela de login**, antes de trocar de tela — a carteirinha nasce sempre pronta, sem estado intermediário visível. Detalhe técnico completo (causas raiz, arquitetura de cache, correção do service worker): `tumtu-documentacao-tecnica.md` seção 24.
✅ **Botões novos da carteirinha + "Trocar de Bateria" funcionando (16/jul/2026)** — rodapé do cartão tem "Meu Perfil" (dourado, ação principal) e "Trocar de Bateria" (borda, só aparece com 2+ vínculos aprovados); topo do cartão voltou a ter só "Sair". Trocar de bateria reaproveita a sessão do Supabase Auth (sem pedir senha de novo) e busca a cor/logo de todas as baterias em segundo plano enquanto a pessoa decide, pra trocar ser instantâneo. **Só a parte do ritmista foi construída** — o fluxo do Admin pós-piloto (cair direto em `admin.html`, "Ver minha carteirinha") continua desenhado mas não implementado, adiado a pedido da Márcia até o piloto estar redondo.
✅ **Tema visual escuro implementado em login/cadastro/Meu Perfil/tela de carregando (17/jul/2026)** — fundo escuro de ponta a ponta, campos de formulário escuros com borda clara (não mais caixa branca flutuando), símbolo da marca + tipografia do logotipo padronizados. A parte de "arte"/identidade visual mais ousada (ilustração ou composição gráfica) foi tentada e pausada por decisão dela — ver `tumtu-documentacao-tecnica.md` seção 26.5.
✅ **Modo Piloto com interruptor de verdade na tela (17/jul/2026)** — antes só existia no banco (`baterias.modo_piloto`), sem controle visual; toda bateria nova nascia com ele desligado por padrão. Agora dá pra ligar/desligar direto em Super Admin → Escolas → aba Bateria.
✅ **Configuração de Medidas por bateria (17/jul/2026)** — mesmo padrão de Instrumentos (biblioteca mestre + ativação por bateria): `medida_tamanhos` + `bateria_medidas`. Resolve a pendência antiga de nomenclatura de tamanho por escola (XXG vs XGG). No caminho, achou e corrigiu uma falha de permissão pré-existente em Instrumentos/baterias (funções de RLS antigas lendo a tabela `ritmistas` congelada) — ver `tumtu-documentacao-tecnica.md` seção 27.
✅ **Tela de carregando no formato final (17/jul/2026)** — depois de testar uma animação do símbolo da marca (revertida por ser rápida demais no celular pra perceber o movimento) e um spinner clássico com logo+texto (inconsistente com outras telas do app), chegou no formato definitivo: spinner dourado sozinho, maior, com brilho suave, sem logo nem texto — referência Netflix/Disney+. Usado em `login.html` e `carteirinha.html` (que também ganhou lógica de nunca mostrar o cartão pela metade — só revela quando nome/foto/cor da escola estiverem 100% prontos). Detalhe completo: `tumtu-documentacao-tecnica.md` seções 24.9-24.10.
✅ **Correções de aprovação de Admin + log de auditoria + PWA (17/jul/2026, sessão à tarde)** — spinner travado pra quem tenta logar pendente (mensagem escrita atrás do spinner, nunca escondido); Dashboard do Super Admin escondia pendências de Mestre/Diretor (filtro só contava ritmista); atalho "X pendente" no Dashboard já abre direto na aba Acessos; Dashboard virou aba inicial do Super Admin (era Escolas). Campo solto "Mestre de Bateria" (texto livre, desconectado do sistema real) removido da aba Bateria. Nova tabela `vinculos_historico_status` + aba "Histórico" no Super Admin registram quem aprovou/rejeitou/mudou status de qualquer vínculo. Nacionalidade virou lista fechada (era texto livre com sugestão). PWA corrigido pra nunca mais precisar apagar e recriar o ícone numa atualização (service worker recarrega sozinho). Detalhe completo: `tumtu-documentacao-tecnica.md` seções 28-29.
🚫 **Carteirinha offline — decisão fechada em 17/jul/2026: NÃO vamos fazer por enquanto.** Lacuna real encontrada e correção prototipada/testada, mas revertida — ela prefere manter atualização automática da biblioteca do Supabase enquanto o app está em desenvolvimento ativo. Não retomar sem ela puxar o assunto. Detalhe: `tumtu-documentacao-tecnica.md` seção 30.
✅ **4 ajustes no formulário de cadastro (17/jul/2026, sessão da noite)** — transição suave nos campos que aparecem/somem (nacionalidade estrangeira, "como se identifica", documento sem CPF, "outro" estado), campo de Instrumento some inteiro da tela pra Admin/Super Admin (nunca fica obrigatório pra quem não tem instrumento), País virou lista fechada de países (era texto livre, "Brasil" continua default), Parentesco do contato de emergência virou lista fechada (Pai/Mãe/Cônjuge.../Outro, sem campo de texto pro "Outro"), e correção automática de maiúscula/minúscula ao sair do campo (nome, endereço, complemento, bairro, cidade, nome do contato — apelido fica de fora de propósito, por causa de abreviações tipo "LC"). Tudo só no cadastro novo, edição de perfil (`ficha-perfil.js`) não é afetada.
✅ **`tumtu-apresentacao.html` (18/jul/2026)** — página avulsa de divulgação (não faz parte do fluxo do app, não está no `APP_SHELL` do PWA), pra Márcia mandar por WhatsApp pra mestres amigos entenderem o que é o TumTu antes de fechar parceria. Rascunho inicial gerado pelo Claude Chat, corrigido aqui: logo errada (bolinhas do símbolo lado a lado em vez da diagonal oficial; traço terracota embaixo de "um" em vez de só "m") e nome do arquivo trocado de "mestres" pra "apresentacao". Layout final: menu do topo e corpo usam só o nome "TumTu" (16px, sem símbolo — padrão do dia a dia do app); rodapé traz o símbolo completo, mas reduzido na mesma proporção do nome pequeno (21×13px em vez do 48×30px oficial de login/cadastro, que é pareado com o nome grande de 36px) — decisão explícita dela depois de eu errar a direção da correção uma vez (tinha aumentado o nome em vez de diminuir o símbolo).

✅ **Redesign completo da carteirinha ("9a"), publicado em produção (11-14/ago/2026)** — foto do ritmista em tela cheia no topo (era circular), bloco central com leve tingimento da cor da escola fazendo transição visual até o rodapé, CPF com dois pontos, barra do topo e cor da borda da logo seguindo as cores reais da escola (nunca mais só as 2 primeiras, nunca mais desequilibradas entre si), QR de emergência com o tamanho certo pra moldura, espaçamento fixo entre logo e QR no verso (não muda mais com 1 ou 2 Mestres). Também foi quando ficou confirmado que o `vercel` CLI não está disponível neste ambiente — o fluxo de aprovação por link de teste (ver regra "Confirmar antes de subir ao GitHub" acima) passou a usar branch de preview + ferramentas MCP da Vercel. Detalhe técnico completo (achados de CSS, regra de cor da borda da logo, workflow novo de preview): `tumtu-documentacao-tecnica.md` seção 32.
✅ **E-mail de "esqueci minha senha" em português (14/ago/2026)** — configurado servidor de envio próprio (Resend) via custom SMTP no Supabase, resolvendo a trava que impedia editar o texto (só usando o servidor genérico do Supabase, sempre em inglês). Detalhe: `tumtu-documentacao-tecnica.md` seção 33.
✅ **Revisão de segurança do fluxo completo de carteirinha (18/jul/2026)** — a pedido da Márcia, auditoria de ponta a ponta (escola→bateria→links de cadastro→aprovação→múltiplos vínculos), direto no banco via Supabase MCP, não só nas telas. Achou e corrigiu **um vazamento crítico**: a view `ritmistas_com_instrumento` (usada por `login.html`/`carteirinha.html`/`admin.html`/`super-admin.html`) estava `SECURITY DEFINER` com `SELECT` liberado pro papel `anon` — qualquer um, sem login, conseguia puxar CPF/endereço/telefone/contato de emergência de todo mundo cadastrado. Corrigido com `security_invoker=true` + revogação de acesso anônimo, testado com contas reais nos 4 níveis (anon bloqueado, ritmista só vê o próprio, admin só vê a própria bateria, Super Admin continua vendo tudo). Também corrigiu um bug real: suspender/desligar alguém no painel Admin não bloqueava o login dela (só mudava o selo visível pra quem gerencia) — `login.html` agora nega acesso por padrão (só `aprovado` passa) e reconfere o status a cada abertura do app, não confia mais em sessão salva antiga. Tela de "escolher bateria" também ficou mais clara: desligado/rejeitado somem da lista, pendente/suspenso aparecem travados com o motivo escrito ao lado. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 31.
✅ **E-mail de "cadastro aprovado" corrigido + ajustes finos de UX/carteirinha (15/ago/2026)** — o e-mail da Edge Function `notificar-aprovacao` não chegava quando quem aprovava era o Super Admin (funcionava no Admin comum): bloqueio de CORS por um cabeçalho (`prefer`) não autorizado pela função, achado via print do Console do navegador da Márcia. Corrigido (função redeployada aceitando esse cabeçalho) e testado com e-mail real. Na mesma sessão: apelido perdeu as aspas em todo canto que aparece (carteirinha, cards de Admin/Super Admin, ficha de perfil) — vira só itálico dourado, sem símbolo (algumas pessoas acharam as aspas "de documento"; testadas e descartadas alternativas com bolinha/traço e fontes diferentes, decisão final foi manter a fonte do app mesmo); botões Salvar/Cancelar da ficha de perfil (Meu Perfil/Admin/Super Admin) ficaram fixos na tela ao rolar; corrigido bug real do "arrastar foto pra reposicionar" (feature de 14/ago) — clicar na foto abria o seletor de arquivo na mesma hora, sem chance de descobrir o arrasto. Handoff externo "redesign v2" da carteirinha (Claude Design) trouxe mais 2 ajustes: traço fino entre escola/bateria e CPF (cor dinâmica, mesma lógica da borda das logos) e verso reordenado pra Logo → Mestre de Bateria → QR (identidade da bateria antes do dado de emergência) — position do QR continua travada como sempre foi (pedido antigo da Márcia), só a distribuição do espaço ao redor do bloco de Mestre foi recalculada pra ficar visualmente equilibrada com 1 ou 2 Mestres. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seções 34-35.
✅ **Barra flutuante do Meu Perfil (3 correções) + e-mail de aprovação reformulado (15/ago/2026, sessão seguinte)** — corrigido corte do botão "Cancelar" no celular (faltava respiro de `env(safe-area-inset-bottom)` pra não ficar atrás do home indicator do iPhone, já que o app instalado é PWA `display:standalone`); clicar na foto não ativa mais a edição de todos os campos (redundante agora que os botões flutuam); "Fechar"/ações extras somem durante a edição. Mensagens de "aguarde aprovação" (cadastro e login) agora avisam sobre o e-mail que chega depois. E-mail de "cadastro aprovado" (`notificar-aprovacao`) ganhou passo a passo de instalação do PWA (Android/iPhone), endereço por extenso além do link clicável (alguns apps abrem link em janela própria, travando a instalação), frase reformulada pra não sugerir que o TumTu concede o cargo, e escola em destaque junto com a bateria (sem duplicar "Bateria" nem colocar entre parênteses) — testado com e-mails reais a cada ajuste, não só a prévia visual. **Símbolo da marca (dois círculos) tentado no e-mail e revertido** — não renderiza direito no Gmail real (`position` é ignorado; tabela HTML também saiu distorcida) — fica só o wordmark "TumTu" por enquanto, retomar com calma depois. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 36.
✅ **Validade da carteirinha configurável por escola — campo provisório (15/ago/2026, sessão seguinte)** — o card já mostrava "Válida até" no verso, mas com data fixa no código (`31/07/2027`, nunca foi real). Nova coluna `escolas.validade_carteirinha` (date), campo em Super Admin → Escolas (junto de "Temporada Atual") — decisão explícita da Márcia de ir pelo caminho rápido agora (reaproveitando o cadastro de escola já existente) em vez de criar já a área dedicada "Configurações → Carteirinha", que fica pra depois (dado já nasce salvo certo, só muda de lugar na tela quando essa área existir). **Só o campo — nenhuma lógica de validade (bloqueio, aviso de vencimento) foi criada ainda**, por pedido explícito dela. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 37.
✅ **Bug real corrigido: QR subia quando a bateria não tinha nenhum Mestre cadastrado (15/ago/2026)** — quebra do requisito explícito da Márcia de QR sempre na mesma posição (13/ago/2026, já protegido antes pros casos de 1/2 Mestres, seção 35). Causa: sem nenhum Mestre, `renderMestres()` deixava o bloco `.v-mestre` com `display:none` — só que é ESSE bloco (via `min-height`) que reserva o espaço fixo até o QR; escondê-lo por completo fazia o espaço reservado sumir junto. Corrigido: o bloco agora fica sempre visível (`display:flex`), vazio quando não há Mestre, preservando o espaço reservado em qualquer cenário. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 38.
✅ **Cadastro/Ficha de perfil: dados do responsável pra menor de idade (17/ago/2026)** — primeiro pedido de um novo apoio, um diretor de bateria real trazendo mais requisitos (mais coisas devem vir). Data de nascimento agora calcula idade no navegador; menor de idade (por enquanto 18 anos — constante isolada `IDADE_MAIOR`, a Márcia ainda vai confirmar o número certo, cogitou 16) revela Nome/CPF do responsável, obrigatórios — tanto no cadastro quanto na ficha de perfil compartilhada (Meu Perfil/Admin/Super Admin, mesma trava de edição de "Data de nascimento": só Super Admin edita). Campos ficam sempre **depois de todos os dados da própria pessoa**, por pedido dela. Novas colunas `pessoas.responsavel_nome`/`responsavel_cpf`. Antes de implementar, avaliação (não implementada) de uma permissão restrita "só ver Ritmistas e aprovar/rejeitar" — achado principal: `vinculos.nivel_acesso`/`nivel_acesso_id` já existem no banco pra isso desde 13/jul/2026, mas nunca foram ligados a nenhuma regra real; complexidade média se ela quiser seguir com isso no futuro. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 39.

✅ **Tema por escola (cor + logo) no cabeçalho do Admin + rodada grande de ajustes + redesign do Exportar Excel (18/ago/2026)** — painel de gestão (`admin.html`) ganhou opção de mostrar cor/logo da escola no cabeçalho, opcional por escola (`escolas.tema_personalizado_ativo`, desligado por padrão), Super Admin nunca afetado. Corrigidos bugs reais achados por ela testando ao vivo: barra de rolagem do navegador cortando por dentro do cabeçalho (`body` virou coluna travada de altura da tela, só `.conteudo-rolavel` rola — não é mais só `sticky`), botão "Editar" da ficha do ritmista que nunca flutuava de verdade pra quem já está Ativo/Suspenso (causa: duas barras `sticky` competindo no mesmo modal), fundo cinza sem explicação atrás dos botões da ficha dentro de modal. Mais uma leva de ajustes: foto do autocadastro virou obrigatória (e passou a ser realmente salva — antes só existia como prévia visual), campo "Celular do responsável", fotos da Diretoria viraram quadradas (como as de Ritmistas), filtro padrão de Ritmistas virou "Ativos + Pendentes" com agrupamento visual por status, Diretoria separada em seções Mestres/Diretores. Modal "Exportar Excel" passou por 3 rodadas de design até aprovar: 1ª tentativa (etiquetas clicáveis + cartões) foi rejeitada na hora ("não tá com cara de algo que possa ser selecionável... antes com os checks estava mais intuitivo, mas estava bagunçado") — checkbox/radio nativos voltaram, só que organizados em grade alinhada + seções com respiro, com atalhos "Marcar todos/Limpar" pro Status e Instrumento; opção "Sem instrumento" removida (instrumento é obrigatório pra todo ritmista). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 40.

🚧 Pendências conhecidas (não urgentes, todas confirmadas como pós-piloto em 17/jul/2026): Apagar a tabela antiga `ritmistas` (risco confirmado baixo — só 2 das 28 linhas são reais, já migradas corretamente; falta só o "pode apagar" final dela). Cadastro de temporadas com histórico e interruptor pra ritmista editar as próprias medidas — 2 ideias registradas, nenhuma desenhada ainda. Área dedicada "Configurações → Carteirinha" (mesmo padrão de Instrumentos/Medidas) — hoje `validade_carteirinha` mora provisoriamente dentro do cadastro de escola (ver seção 37 da documentação técnica), a Márcia já sinalizou que quer migrar pra lá quando essa área existir.

✅ **Trocar cargo — Diretor/Mestre virando Ritmista (18/jul/2026)** — resolvida a pendência da revisão de segurança acima. Implementado em `super-admin.html`, aba Acessos → editar → campo "Perfil" ganhou a opção "Ritmista", pedindo o instrumento na hora (Admin nunca preenche isso no cadastro). Escopo só nessa direção — promover Ritmista pra Diretor fica pra quando entrar o projeto de unificação admin/super-admin (ver roadmap abaixo).

✅ **Fundamentos de LGPD (19-20/jul/2026)** — a pedido da Márcia, depois de uma conversa externa sobre risco de usar a marca da escola sem contrato de parceria. Criada `politica-privacidade.html` (linguagem simples, sem juridiquês — o que os dados fazem, quem vê, quanto tempo fica, direitos da pessoa, com destaque pra exceção do QR de emergência sem login). Cadastro (`cadastro.html`) ganhou checkbox obrigatório "Li e concordo com a Política de Privacidade" (só no autocadastro, não no modo manual — ali quem confirma é o admin via `consentimento_confirmado`) — `vinculos.politica_privacidade_aceita_em` registra quando aceitou, prova de consentimento. Criado `config-suporte.js` (e-mail centralizado, mesmo padrão do `config-escola.js`) — `suporte@tumtu.com.br`, encaminhamento grátis via ImprovMX pro `tumtuapp@gmail.com`, sem caixa de entrada nova pra checar. Link "Fale com o suporte" adicionado no rodapé de login/cadastro (fora do formulário, visível desde o passo 1) e dentro de "Meu Perfil" (motor único, aparece em carteirinha/admin/super-admin de uma vez) — estilo discreto de propósito, pra não competir visualmente com as ações principais da tela.

✅ **Exclusão de dados sob pedido — LGPD (20/jul/2026)** — nova aba "Privacidade" em `super-admin.html`: busca pessoa por nome/CPF/e-mail (acha até quem não tem vínculo ativo hoje, ex: órfã de escola já excluída) → excluir ficha + conta de login; ou seleciona uma escola inteira → apaga escola, baterias e todos os vínculos ligados a elas (fichas pessoais de quem ficar "órfão" são preservadas — dado é da pessoa, não da escola). Confirmação exige digitar "EXCLUIR". Duas funções SQL atômicas no banco (`excluir_pessoa_lgpd`, `excluir_escola_lgpd`, SECURITY DEFINER) + duas Edge Functions (`admin-excluir-pessoa`, `admin-excluir-escola`, só Super Admin, apagam a conta de login por último de propósito — pior cenário de falha parcial é sobrar login vazio, nunca CPF sem controle de acesso). Nova tabela `exclusoes_lgpd`: log de prova imutável (nome em texto, CPF só como hash irreversível salgado, nunca o dado sensível de volta) — sem policy de UPDATE/DELETE pra ninguém, nem Super Admin; só a Edge Function grava, via service_role. Nunca permite excluir conta de Super Admin (bloqueado dentro da própria função SQL, não só escondido na tela). Testado de ponta a ponta com dados descartáveis reais antes de publicar: pessoa (ficha+vínculo+login some, login realmente para de funcionar depois), escola (bateria+vínculos somem, ficha da pessoa sobrevive como esperado), e a trava de Super Admin (recusa corretamente).

(Duas pendências antigas — "Validação Digital" e padding do "Membro desde" — **confirmadas obsoletas em 20/jul/2026**: já não existem mais no código, foram resolvidas por algum redesign anterior sem eu ter atualizado o registro.)

✅ **Bloqueio de cadastro duplicado por documento, sem CPF (20/jul/2026)** — a checagem de duplicidade em `cadastro.html` (função `verificar_pessoa_existente`) já impedia CPF repetido com e-mail diferente, mas não tinha equivalente pra estrangeiro sem CPF (usa Passaporte/RNE + número em vez de CPF) — essa pessoa podia se cadastrar de novo trocando só o e-mail, sem barreira nenhuma. Função SQL ganhou `p_numero_documento` + `existe_documento`/`documento_bate_email`, mesmo padrão do CPF; `cadastro.html` bloqueia com a mesma mensagem ("já tem cadastro com outro e-mail... Esqueci minha senha"). Cadastro comum com o mesmo e-mail duas vezes continua coberto (RLS + Supabase Auth), então esse ajuste fecha só a lacuna específica de documento alternativo.

✅ **Três ajustes da carteirinha/cadastro (22/jul/2026)** — 1) Título do convite de cadastro (`cadastro.html`) não repete mais "bateria" duas vezes ("Cadastro de Diretor da bateria Bateria do Jacarezinho" → "Cadastro de Diretor da Bateria do Jacarezinho"). 2) Carteirinha e login (`carteirinha.html`, `login.html`) agora mostram a **sigla** da escola (`escolas.sigla`) em vez do nome completo. 3) Exclusão de pessoa/escola sob LGPD (`super-admin.html`, aba Privacidade) passou a **exigir motivo** pra confirmar — pra pessoa, um seletor fixo ("A pedido da própria pessoa" / "Perda de acesso — vai se cadastrar de novo" / "Outro" com texto livre); pra escola, texto livre continua, só que agora obrigatório.

**Decisão importante sobre senha em cadastro feito por outra pessoa (22/jul/2026) — NÃO implementar reset de senha pelo Super Admin, mantém a regra ética intacta.** A Márcia cogitou 2 ideias (senha automática = CPF ao cadastrar alguém; Super Admin resetar senha de quem perdeu acesso) e decidiu não seguir nenhuma das duas depois de conversarmos sobre os riscos (CPF como senha é recuperável a partir do próprio banco; reset pelo Super Admin quebraria a regra "nunca define senha de outra pessoa"). Solução adotada, sem nenhuma mudança de código: **no cadastro manual, quem está cadastrando preenche tudo e entrega o aparelho pra própria pessoa digitar a senha no campo, sem ver**; **se a pessoa perder acesso total ao e-mail (não consegue nem recuperar senha), o caminho é excluir a ficha dela (aba Privacidade) e recadastrar do zero** — perde o histórico de aprovação/vínculo, mas é caso raro. O log `exclusoes_lgpd` prova que a exclusão aconteceu (nome + CPF em hash), mas não é backup — não existe "desfazer".

📦 Backlog "só se o TumTu crescer muito" (itens rebaixados por decisão explícita da Márcia em 17/jul/2026 — não tratar como prioridade nem sugerir de novo sem o cenário mudar): "Leaked Password Protection" (trava de senha vazada, depende de plano pago do Supabase, que ela não pretende contratar por enquanto).

**Face ID pra abrir o app — reativado como interesse real em 19/ago/2026** (tinha sido rebaixado pro backlog acima em 17/jul, mas ela voltou a perguntar e confirmou interesse). Análise de viabilidade já feita: funciona em iPhone e Android via WebAuthn (recurso padrão do navegador, não precisa virar app de loja), tamanho médio-pequeno (o app já mantém sessão salva — só falta colocar Face ID como confirmação na frente disso, não vai ser preciso descartar a sessão que já existe), único risco real é comportamento de WebAuthn dentro do PWA instalado no iPhone variar por versão do iOS — precisa testar em aparelho real antes de prometer. **Ordem combinada por ela: primeiro terminar o que falta da gestão do sistema (unificação Admin+Super Admin e pendências relacionadas), Face ID vem depois.** Não é backlog "só se crescer" — é próximo passo real, só depois da fila atual.

## Roadmap combinado com a Márcia em 10/jul/2026 (ordem definida por ela)
Depois do domínio: 1) revisão de todas as telas (correções gerais) → 2) revisão de layout com visão de UX expert → 3) **inclusão de instrumentos (urgente, logo após a revisão de telas)** → 4) lógica de temporada em relação a ritmistas → 5) controle de camisas por temporada (múltiplas entregas na mesma temporada, não só "marcar quem recebeu"). Depois de tudo isso: fase de marketing, começando por e-mail com o domínio próprio. **Itens 1, 2 e 3 já concluídos** (revisão de telas + UX em 10/jul, ver seção 19 da documentação técnica; instrumentos configuráveis em 11/jul, ver linha do tempo na seção 21). **Itens 4 e 5, confirmados pós-piloto em 17/jul/2026** — ela mesma concordou que o valor desses dois só aparece com histórico de mais de uma temporada, e o piloto está só começando. Fora da ordem original, mas resolvido com urgência por ser bloqueador do piloto: a migração pessoa/vínculo (13/jul/2026, seção 22 da documentação técnica).

**Sequência combinada em 14–17/jul/2026, específica da carteirinha/piloto (prioridade dela, fora do roadmap de 10/jul acima) — CONCLUÍDA:** 1) cor/logo por escola ✅ 15/jul → 2) botões novos da carteirinha (Meu Perfil + Trocar de Bateria) ✅ 16/jul → 3) redesign visual de login/cadastro/perfil pra "cara de app moderno" ✅ 17/jul (fundo+campos escuros implementados; parte de "arte" ilustrada pausada por decisão dela) → 4) configuração de medidas (mesma lógica dos instrumentos) ✅ 17/jul. Fluxo de login pós-piloto completo pro Admin (cair direto em `admin.html`) fica pra **depois do piloto estar redondo**, decisão explícita dela em 16/jul — "o piloto é o piloto".

**Próximo grande marco, em andamento desde 21/ago/2026:** a Imperatriz Leopoldinense (`escolas.id=2`, bateria "Swing da Leopoldina", já marcada `tipo=real` desde a feature de 19/ago) vai ser a primeira escola/bateria real de verdade — ela sinalizou estar "bem próxima" de lançar. **Pendência conhecida antes do lançamento**: essa mesma escola foi usada pra testar features ao longo de várias sessões, então tem 17 vínculos hoje, quase todos claramente fake (nome/e-mail `@teste.tutti`) — a Márcia vai marcar essa escola como `demo` por conta própria (não pedir pra fazer isso por ela) e, quando estiver pronta, vai pedir explicitamente pra excluir **"Jhones Pereira de Souza Silva"** (`jhones_thuran@hotmail.com`, pessoa_id 98 em 21/ago/2026), que parece ser uma pessoa real que se cadastrou por engano/teste, não um dado fake — **não excluir sem ela pedir de novo, mesmo sabendo que é isso que ela quer**, porque exclusão de pessoa é irreversível (aba Privacidade, seção 20/jul/2026). Detalhe técnico importante achado nessa conversa, pra não redescobrir do zero: `excluir_escola_lgpd()` apaga vínculos/bateria/histórico de status (cascade), mas **não apaga as fichas de pessoa** (`pessoas`) — ficam órfãs de propósito ("dado é da pessoa, não da escola"). Pra realmente zerar tudo (o que ela quer: "não quero que tenha histórico, nada"), a ordem certa é excluir as pessoas primeiro, a escola depois.

**Nova frente combinada em 18/jul/2026, depois dos gaps da carteirinha — CONCLUÍDA em 19/ago/2026:** unificar `admin.html` e `super-admin.html` numa fachada só, com abas que aparecem/somem por permissão. Publicado em produção: `super-admin.html` foi apagado, `admin.html` é agora o único arquivo (Super Admin ganhou barra lateral própria dentro dele). Nesse caminho, o modelo de permissão original (perfil nomeado, nunca usado de verdade) foi **substituído** por um modelo direto por pessoa, bem diferente do que estava previsto quando essa frente foi combinada em 18/jul — detalhe completo, incluindo um incidente de segurança achado e corrigido no meio do trabalho (view `ritmistas_com_instrumento` perdendo `security_invoker=true` ao ser alterada — mesma proteção da seção 31), em `tumtu-documentacao-tecnica.md` seção 41.

✅ **Unificação Admin + Super Admin, permissões reais e Escolas DEMO x reais (19/ago/2026)** — `admin.html` virou o único painel de gestão do TumTu; `super-admin.html` foi apagado. Super Admin ganhou barra lateral própria (Dashboard/Escolas/Configurações/Privacidade); dentro de uma escola, todo mundo usa a mesma barra de abas horizontal, com módulos extras (Dados da Escola, Dados da Bateria, **Comercial** — só o interruptor Modo Carteirinha, separado por ser decisão comercial —, Permissões, Histórico) liberados por capacidade, não mais por arquivo/papel fixo. Permissões passaram a ser **atribuídas direto por pessoa** (`vinculos.capacidades`, jsonb — 18 chaves `ver_X`/`editar_X`), não por perfil nomeado — a Márcia testou o modelo antigo (que já existia pronto no banco, nunca usado) e pediu esse redesenho no meio da própria sessão. Trava real no banco (RLS + `tenho_capacidade()`), testada tentando furar via API direto, não só escondendo botão. **Achado e corrigido um incidente de segurança no caminho** (a view `ritmistas_com_instrumento`, protegida desde julho contra o vazamento anônimo da seção 31, perdeu essa proteção ao ser alterada para a nova coluna — religada e reconfirmada antes de qualquer prévia ser mostrada). Nova coluna `escolas.tipo` (real/demo) separa escolas de demonstração das reais na lista e no Dashboard. Detalhe técnico completo, **inclusive a explicação de por que esse tipo de incidente pode se repetir em mudanças futuras de view e como evitar** (regra a seguir sempre): `tumtu-documentacao-tecnica.md` seção 41.

✅ **Apoio de Bateria (novo perfil), Modo Carteirinha individual e Diretor de Naipe/Repique de Bossa (21/ago/2026)** — três frentes que ela tinha deixado pra depois da unificação, fechadas juntas numa sessão de planejamento + implementação direta (ela pediu explicitamente pra agilizar e publicar direto, sem prévia passo a passo). **Apoio de Bateria**: novo `perfil` (`vinculos_perfil_check` + RLS + trigger de edição atualizados), tratado em pé de igualdade com Mestre/Diretor em todo canto — seção própria na aba Diretoria, link de autocadastro (`?cargo=apoio`), capacidades configuráveis em Permissões. **Modo Carteirinha individual** (`vinculos.modo_carteirinha_individual`, coluna própria, fora do jsonb de capacidades): Mestre/Diretor/Apoio específico pode ser marcado pra só ver a própria carteirinha, nunca o painel — Ritmista já tem isso embutido, sem marcação. **Diretor de Naipe** (`vinculos.naipe`, array de instrumentos + pseudo-opções "Repique de Bossa"/"Especiais"): não é perfil novo, é atributo de um Diretor já existente, com regra de selo combinada (Surdo Primeira+Segunda → "Surdo de Marcação"; 2+ variantes de Repique → só "Repique"; uma opção sozinha mostra o nome literal) — selo aparece só no painel, nunca na carteirinha; hoje só autoeditável ou por Super Admin (não existe ainda uma capacidade "editar outro Mestre/Diretor/Apoio"). **Repique de Bossa** (`vinculos.repique_bossa`, boolean): grupo especial dentro do Ritmista de Repique/Repique Mor, não vira instrumento novo na biblioteca mestre — o card do Ritmista mostra os **dois** selos juntos (instrumento + Repique de Bossa, achado dela depois da primeira versão que só trocava um pelo outro); a carteirinha, por decisão explícita dela (espaço fixo do cartão), continua mostrando só um. No caminho, corrigida uma lacuna real deixada na implementação do Apoio: `ficha-perfil.js` não tratava `apoio` nos mesmos lugares que já tratava mestre/diretor, então um Apoio com `editar_ritmistas` liberado não via Instrumento/Medidas como editável na tela (o banco já permitia, só a tela que escondia). Duas migrações de banco recriaram a view `ritmistas_com_instrumento` (pras colunas novas) — `security_invoker=true` reafirmado e reconferido nas duas, mesma disciplina da seção 41. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 42.

✅ **Ajustes finos: Diretoria como módulo inteiro, card da Visão Geral e título de coluna em Instrumentos (21/ago/2026)** — pedidos pontuais da mesma sessão. Card "Diretoria e Apoio ativos" (criado nessa sessão) virou só "Diretoria ativa" — a contagem já somava Mestre+Diretor+Apoio desde o início, só o nome mudou. Ícones de aniversário (🎂) e nacionalidade estrangeira (🌍), que já existiam no card de Ritmistas, passaram a aparecer também nos cards de Mestre/Diretor/Apoio — pedido dela: "diretoria" pra ela é o módulo inteiro, não só uma parte. O widget "Aniversariantes do mês" da Visão Geral passou a somar Ritmistas + Diretoria. Configurações → Instrumentos ganhou título de coluna ("Instrumento" / "Nome usado") pros dropdowns de nomenclatura, mesmo padrão visual já aprovado em "Vagas de Ritmistas" (19/ago/2026). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 43.

✅ **URLs sem `.html` no endereço (21/ago/2026)** — pedido dela depois de reparar que todo endereço do site terminava em `.html` ("qualquer um vai saber a tecnologia que eu usei"); esclarecido antes de mexer que não é falha de segurança nenhuma, só estética. Ativado `cleanUrls: true` num `vercel.json` novo (recurso nativo da Vercel — redireciona `/x.html` → `/x` em qualquer endereço do site, sozinho, sem precisar listar arquivo por arquivo) — testado direto numa prévia via `curl` antes de publicar (redirecionamento, querystring preservada, arquivos que não são `.html` intocados). Mas o redirecionamento automático só resolve *depois* de alguém clicar — o **texto** de um link continua mostrando `.html` até clicarem nele — então todo lugar que gera ou mostra um endereço foi limpo pra já nascer sem a extensão: os links de cadastro por bateria (Mestre/Diretor/Apoio/Ritmista) que ela copia e compartilha, o QR de emergência da carteirinha, toda navegação interna do app, o `manifest.json` do PWA (`start_url` — o que abre ao tocar no ícone instalado), a lista de cache do `sw.js`, e o e-mail de "cadastro aprovado" (Edge Function `notificar-aprovacao`, redeployada). **Não coberto**: o e-mail de "esqueci minha senha" é um template nativo do Supabase Auth (painel deles, fora deste repositório) — sem ferramenta de acesso a esse conteúdo nesta sessão, sinalizado a ela pra conferir por conta própria se precisar. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 44.

✅ **Redesign de Configurações → Instrumentos + correção de texto de Vagas + ID visível (21/ago/2026)** — primeira versão do título de coluna em Instrumentos (item acima) não resolveu de verdade: ela apontou que "Instrumento / Nome usado" empilhado com "Tradicionais" ainda lia como dois títulos confusos, e um ajuste só de espaçamento não foi o suficiente ("eu esperava mais de você"). Refeito: cada instrumento virou um `.item-card` de verdade (mesmo componente de Vagas/Diretoria/Escolas) em vez de linha própria; cabeçalho de coluna removido por completo; quando um instrumento tem mais de um nome possível, o rótulo "Nome usado" mora dentro do próprio card, coladinho no dropdown. Título de grupo (Tradicionais/Especiais) passou a reaproveitar `.secao-titulo` (já usado em Diretoria), com borda + respiro reais entre um grupo e outro (segundo ajuste dela, depois de ver a primeira versão do redesign). Frase de Vagas de Ritmistas corrigida: "Use 0 para sem limite" virou "Deixe em 0 enquanto ainda não tiver decidido o limite" — o comportamento (mostrar "-" em vermelho, pressionando a preencher) já estava certo desde 19/ago/2026, só o texto que descrevia errado, dando a entender que 0 era uma escolha permanente. "Dados da Escola"/"Dados da Bateria" ganharam campo ID (somente leitura) — pedido dela pensando em como distinguir duas escolas com nome igual (ex: uma demo e outra real), usando o número interno que já existia no banco mas nunca aparecia em tela nenhuma.

✅ **"Ritmistas por Instrumento" mostra naipes vazios + aviso "Faltam N"/"Sem definição de vagas" + Instrumentos com colunas de verdade alinhadas (21/ago/2026)** — sequência de ajustes na mesma sessão, cada um testado numa prévia antes do próximo. A lista "Ritmistas por Instrumento" da Visão Geral antes só mostrava instrumento que já tinha gente cadastrada — naipe vazio simplesmente sumia; ela quer que apareça mesmo com zero gente, "caso queira chamar atenção da galera". Junto, aviso discreto ao lado da pílula de contagem: "Faltam N" (vaga definida, ainda não completa) ou "Sem definição de vagas" (nunca configurada) — mesmo aviso replicado em Configurações → Vagas de Ritmistas (função `avisoVagaHtml()` compartilhada entre as duas telas). Passou por várias rodadas de ajuste fino de alinhamento: a causa raiz de "está desalinhado" era a pílula de contagem da Visão Geral nunca ter tido largura mínima fixa (diferente da de Vagas, que já tinha) — números diferentes ("3/5" vs "12/8") mudavam o tamanho da pílula e empurravam o aviso pra uma posição diferente em cada linha; corrigido com `min-width` igual nas duas telas. Configurações → Instrumentos teve o mesmo problema um passo adiante: dropdowns de nomenclatura se ajustavam ao próprio texto ("Caixa" bem mais estreito que "Surdo de Primeira"), e o nome repetido (pra quem só tem uma opção) não tinha caixa nenhuma — corrigido com largura fixa (170px) e mesmo estilo de caixa nas duas variantes, texto alinhado à esquerda dentro delas. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 47.

✅ **Medidas: liga/desliga por tipo inteiro (22/ago/2026)** + **rodada grande de polimento visual do Admin (23/ago/2026, manhã/tarde)** — nova tabela `bateria_medida_tipos` deixa cada bateria desligar um tipo de medida inteiro (não só tamanho por tamanho), mesmo padrão "sem linha ainda = ativo por padrão" de Instrumentos (base do que a seção 49 da documentação técnica tornaria totalmente aberto no dia seguinte). Três rodadas de ajuste visual publicadas em sequência: menu lateral dourado sólido + botão Sair padronizado + Visão Geral em 2 colunas + totalizador de Vagas + exportação de Diretoria igualada a Ritmistas + atalho de Permissões na ficha + logo dinâmica em Escolas; depois, padronização completa de título de seção (linha dourada em todo canto, respiro fixo 6/12/24px, cor unificada, correção de dois casos de "respiro dobrado"); depois, o mesmo respiro de 24px estendido a `.ficha-secao` (Dados da Escola/Bateria, Privacidade, Diretoria) + correção de cards colados em 4 listas que nunca tiveram a classe `lista-cards` (achado real dela, 5ª vez apontando o mesmo tipo de problema — reforça auditar a categoria inteira via grep, não só o exemplo citado). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 48.

✅ **Cabeçalho do painel: três iterações até a versão final com a sigla (23/ago/2026)** — passou por "bateria em destaque, escola como legenda" (pedido inicial dela: "o sistema é sobre gerir a bateria, ela devia ter mais destaque"; nasceu aqui a coluna opcional `escolas.nome_curto`, editável em Dados da Escola, pra tirar prefixos formais tipo "G.R.E.S." quando preenchida) → "escola em cima, bateria embaixo em caixa alta dourada" (Option G, aprovada e publicada) → de volta pra **sigla completa** ("G.R.E.S. Imperatriz Leopoldinense (T)"), decisão final dela depois de comparar as versões ao vivo — "eu sinceramente acho que fica mais bonito". `nome_curto` continua existindo (campo salvo, editável), só não entra mais no cabeçalho — guardado por decisão dela pra um uso futuro ainda não definido. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 48.5.

✅ **Reforma de Medidas: sistema totalmente aberto — item "7 completo" do backlog (23/ago/2026)** — Camisa/Fantasia/Calça/Sapato deixam de ser 4 tipos fixos no código (texto com `CHECK` no banco, 4 `<select>` escritos à mão em cada tela) e viram biblioteca mestre aberta, o mesmo padrão já usado em Instrumentos: o Super Admin cria/edita/desativa tipos de medida a qualquer momento em Configurações → Medidas, sem nenhuma mudança de código, e **todo tipo novo já nasce obrigatoriamente ligado à própria escala de tamanhos** — pedido explícito dela, travado em validação ("Adicione pelo menos um tamanho — toda medida precisa de uma escala de tamanhos"). Antecipado por decisão dela ("Não vou esperar. Vamos fazer logo. Sei dos riscos"), fora da ordem original que previa esperar o cadastro real da Imperatriz assentar — o cadastro ainda não tinha começado nesse dia. Cobre ficha de perfil (`ficha-perfil.js`, motor único), Configurações → Medidas por bateria, a nova tela do Super Admin "Tipos de Medida" (tipo + escala de tamanhos num editor só, mesmo padrão de Categoria+Nomenclaturas de Instrumentos), cadastro público (campos montados em tempo de execução) e exportação Excel. Banco de dados migrado e verificado **antes** de qualquer tela ser tocada (backup completo + comparação campo a campo de 56 vínculos / 188 valores, zero diferença) — colunas antigas mantidas como rede de segurança, sem uso. Testado ao vivo de ponta a ponta antes de qualquer prévia ser mostrada, achando e corrigindo 3 bugs reais no processo (campo Nome que se apagava sozinho no editor de tipos; tipo novo que não aparecia em bateria nenhuma até alguém mexer manualmente no interruptor, por não seguir a convenção "sem linha ainda = ativo" já usada no resto do sistema — corrigido tanto em `ficha-perfil.js` quanto na view pública `bateria_medidas_publicas`; RLS de `vinculos_medidas` que na primeira versão bloquearia até a própria pessoa preenchendo a própria medida, corrigido pra espelhar o mesmo grão grosseiro já usado em `vinculos`). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 49.

✅ **Figurino (catálogo + entrega), interruptor "ritmista edita medidas em branco" e bug real corrigido (23/ago/2026)** — item 10 do backlog (renomeado "vestimentas de evento" → **Figurino**), fechado junto com a pendência acima (ritmista editar a própria Medida). Desenho inicial (**nomenclatura corrigida no dia seguinte, ver entrada 24/ago logo abaixo — "Figurino Pai/Filho" não é mais o termo certo**): peça de evento (ex: "Camisa da Final") cadastrada dentro de um tipo de Medida existente só por referência de tamanho — **nunca tem tamanho próprio, só rastreia entregue/não entregue**. Ficha da pessoa ganha "Entrega de Figurinos", resumo 100% de leitura. Capacidade nova `ver_figurino`/`editar_figurino`. Resolve também a pendência antiga: novo interruptor por bateria em Permissões → "Ritmistas" ("Permitir que o ritmista edite as medidas") — quando ligado, ritmista só **preenche** campo de Medida em branco, nunca edita de novo depois de salvo (travado em RLS de verdade: só existe policy de INSERT pro ritmista em `vinculos_medidas`, nunca UPDATE/DELETE). **Bug real achado e corrigido no caminho**: editar qualquer ficha (não só Medidas) e salvar sem mexer no campo "Parentesco" apagava silenciosamente o valor sempre que era texto antigo de antes da lista fechada existir (ex: "Esposa") — 16 pessoas afetadas no banco, incluindo 1 Mestre real da Imperatriz. Corrigido preservando o valor legado como opção extra no `<select>`, testado ao vivo. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 50.

✅ **Dashboard zerado sem bateria real + Figurino ganha lista mestre e nomenclatura correta (24/ago/2026)** — dois ajustes na mesma sessão. **Dashboard**: sem nenhuma bateria real cadastrada, os cards de Escolas/Baterias/Pessoas ativas/Pendências sumiam por completo em vez de mostrar zero — corrigido, agora sempre mostram o número real (incluindo zero). **Figurino, reformulado**: ela apontou que o modelo do dia anterior tinha 3 problemas — nome errado, sem lista mestre, e Ritmista/Diretoria misturados numa peça só. Nomenclatura oficial agora: **Categoria de Figurino** = tipo de peça (Camisa, Calça...; era chamado errado de "Medidas"/"Tipos de Medida", só o rótulo mudou); **Figurino** = peça específica (ex: Camisa da Final, agora com lista mestre própria — Super Admin cadastra uma vez com Categoria + Público, cada bateria só ativa o que usa); **Medida** = tamanho de cada pessoa, continua exatamente como já era na ficha. Peça de Figurino nunca mistura públicos diferentes — tela de entrega (`Mais → Entrega de Figurino`, renomeada pra não duplicar nome com `Configurações → Figurino`) separa por público, agrupado por Categoria dentro de cada um (**Público virou granular no dia seguinte, ver entrada logo abaixo — não é mais só Ritmista/Diretoria**). **Achado no caminho**: Categoria de Figurino (ex-Medidas) também nascia ligada por padrão em toda bateria nova — herança de quando Camisa/Fantasia/Calça/Sapato eram fixas no código, sem sentido depois da reforma de 23/ago (item 7). Corrigido pro mesmo padrão de Instrumentos (nasce desligada), com backfill de segurança gravando o estado atual de toda bateria existente antes da troca — zero mudança de comportamento pra quem já estava configurado. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 51.

✅ **Figurino: público granular (Ritmista/Mestre/Diretor/Apoio) + título livre no Exportar Excel (24/ago/2026)** — dois ajustes pedidos na sequência, depois dela ver a entrada acima publicada. **Público granular**: `figurino_itens_mestre.publico` deixa de ser um bloco agrupado "diretoria" e passa a aceitar os 4 valores de perfil (ritmista/mestre/diretor/apoio) — pedido dela pensando numa bateria que no futuro queira peça diferente por Mestre/Diretor/Apoio ("quero estar preparada pra isso"). Todas as telas de Figurino (Super Admin, Configurações da bateria, Entrega) agrupam nos 4 públicos, sempre Ritmistas primeiro. **Título livre no Exportar Excel**: campo de texto opcional "Título do relatório" no modal — preenchido, define tanto o título dentro da planilha quanto o nome do arquivo baixado (sanitizado); em branco, funciona exatamente como já era. Ela cogitou (e descartou sozinha) uma versão automática amarrada ao Figurino antes de chegar nessa — texto livre puro, sem lógica nenhuma de Figurino, porque um relatório pode ser sobre qualquer coisa (seu exemplo: "um relatório do endereço dos ritmistas... não faz sentido ter esse tipo de título"). Caso de uso real: Diretor exporta o grupo "Medidas" (já existente) com o título "Camisas da Final" pra mandar a lista de tamanho pra fábrica. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 52.

✅ **Aniversariantes com cargo/gênero, cores padronizadas na Visão Geral, resumo de entrega de Figurino, 2 bugs reais (24/ago/2026, sessão seguinte)** — "Aniversariantes do mês" sempre mostra o cargo (Ritmista/Mestre/Diretor/Apoio, com instrumento junto quando existe); achados e corrigidos junto mais 3 lugares do painel que mostravam "Mestre"/"Diretor" sem seguir o gênero da pessoa (só existia em `fpCargoLabel`, usada na própria ficha). Visão Geral: "Pendentes" fica terracota (mesma cor do card do Dashboard) e "Ativos" fica verde (mesmo tom do selo "Ativo") — reaproveitando cor que já significa a mesma coisa em outro canto do app, pedido dela nas duas vezes. **Resumo de entrega de Figurino**: novo card na Visão Geral com o progresso de cada peça ativa, quebrado por naipe pra peça de Ritmista (mesmo formato de "Ritmistas por Instrumento", mas o denominador é gente ativa do naipe, não vaga) — passou por 2 versões até fechar: a 1ª (um interruptor "Entrega encerrada", nascendo já visível) foi rejeitada por ela depois de eu perguntar se estava concluído ("se eu tiver 30 criadas e não encerradas vão aparecer as 30... configurar com antecedência não quer dizer que eu quero mostrar"); a versão final tem **dois interruptores independentes**, os dois nascendo desligados: "Mostrar na Visão Geral" (ela liga quando quer acompanhar) e "Entrega Finalizada" (esconde de novo quando não sobra mais ninguém pra entregar). **Dois bugs reais corrigidos**: menu do rodapé no celular tinha o nome da aba selecionada invisível (texto escuro pensado pra pílula dourada, sobre fundo escuro sem a pílula) — vira dourado; foto do Super Admin nunca aparecia na bolinha do cabeçalho, por dois bugs encadeados (login nunca buscava `foto_url` do Super Admin, e editar Meu Perfil não atualizava a bolinha na mesma sessão). Detalhe técnico completo (inclusive o desenho descartado do Figurino e como o mal-entendido foi resolvido): `tumtu-documentacao-tecnica.md` seção 53.

✅ **Categoria de Figurino Tradicionais/Especiais, Visão Geral empilhada e polimento de Entrega de Figurino (24/ago/2026, sessão seguinte)** — três frentes pontuais. **Categoria de Figurino** (Camisa/Fantasia/Calça/Sapato...) ganhou grupo Tradicionais/Especiais, mesmo rótulo visual de Instrumentos (`medida_tipos.grupo`): tradicionais continuam obrigatórios no cadastro como sempre foram, mas uma categoria pontual criada no meio da temporada (ex: "Vestido", só pro naipe do Chocalho) pode nascer Especial — opcional, sem forçar quem não usa a preencher. No caminho, corrigido um bug real (seleção de Grupo perdida ao clicar "+ Adicionar tamanho" no editor). **Visão Geral**: os cards (Aniversariantes, Ritmistas por Instrumento, Entrega de Figurino) deixaram de ficar em grid de 2 colunas — o card mais curto deixava um vão vazio embaixo sempre que o outro crescia mais — e passaram a empilhar, na ordem Aniversariantes → Ritmistas por Instrumento → Entrega de Figurino. **Entrega de Figurino**: tela de detalhe de uma peça ganhou cabeçalho de coluna "Tamanho"/"Status" (mesmo padrão de Vagas de Ritmistas) com o tamanho de cada pessoa dentro de uma caixinha com borda, respiro de 32px entre as colunas, e o interruptor "Mostrar na Visão Geral" renomeado pra "Entrega Iniciada" (só o rótulo — id/função/coluna no banco continuam iguais). Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 54.

✅ **Auditoria de segurança pós-grandes-mudanças + 6 vazamentos corrigidos (24/ago/2026, sessão seguinte)** — pedido dela antes de liberar o sistema pra uso real: revisitar vazamento de dados depois de tantas mudanças no banco, mesma disciplina de sempre (seções 31/41). Achado **crítico**: `excluir_pessoa_lgpd`/`excluir_escola_lgpd` nunca checavam quem estava chamando — qualquer pessoa sem login conseguia apagar qualquer pessoa ou escola do sistema inteiro via API, de forma irreversível. Achado **alto**: o QR de emergência (`ritmistas_emergencia`) aceitava qualquer `pessoas.id` sequencial sem checar nada — dava pra "varrer" nome/tipo sanguíneo/contato de emergência de todo mundo já cadastrado trocando o número na URL; trocado por `pessoas.qr_token` (uuid aleatório) — no caminho, achado que o QR também rodava com o id errado (`vinculo_id` em vez de `pessoa_id`), corrigido junto. Achados **médio/baixo**: um oráculo CPF→nome/e-mail sem uso real (execução revogada) e duas views sem filtro nenhum (`baterias_publicas`, `mestres_publicos`) que deixavam listar todas as baterias/Mestres do sistema numa consulta só sem login — viraram funções que exigem o identificador certo como parâmetro. Todos os 6 confirmados corrigidos com chamada HTTP real usando a chave pública (deve negar) e o caminho legítimo (deve continuar funcionando), depois testado ao vivo no navegador (carteirinha, QR, cadastro por link fixo) antes de publicar. **Nenhum dado real foi exposto durante a auditoria.** Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 55.

🚧 **Backlog real, não documentado até agora: limpeza manual do bug "CRÍTICO: medida do Ritmista se perdia no auto-cadastro" (achado dela no lançamento real da Imperatriz, corrigido no banco por volta de 23/ago/2026 — RLS de `vinculos_medidas` só liberava a gravação da própria medida se o interruptor "Permitir que o ritmista edite as medidas" já estivesse ligado, e ele nasce desligado por padrão).** A correção impede a perda de dado em cadastros novos, mas não recupera quem já tinha se cadastrado ANTES da correção com a medida silenciosamente descartada (a pessoa via "Cadastro enviado com sucesso!" sem saber que a medida não tinha sido salva). Ela está pedindo pro grupo da bateria reenviar manualmente, um por um — sem esse relato dela em 25/ago/2026 ("me deu e está dando uma dor de cabeça danada... tive que pedir pra pessoas entrarem e editarem as medidas de novo, um horror"), esse acompanhamento nunca tinha sido registrado aqui. Situação em 25/ago/2026: começou em 37 dos 111 Ritmistas ativos da Imperatriz com Medida totalmente em branco (não é parcial — quem tem pelo menos 1 medida salva tem as 4 completas); ela foi entrando em contato e editando ela mesma ao longo do dia, caiu progressivamente até restar só **2** (Gabriel Finizola André, Lucas Antunes Ribeiro) na última conferida da sessão. **Se ela pedir de novo uma contagem de quem ainda falta**, a consulta é: Ritmistas aprovados da bateria com zero linhas em `vinculos_medidas` (ver seção 56.7 da documentação técnica).

✅ **Sessão de 25/ago/2026: soluço de infraestrutura, causa real da lentidão (fotos sem cache), gatilho bloqueando escrita em `baterias`, logo da Swing corrigida, Aniversariantes com totalizador** — sessão longa, disparada por ela reportando "Ativar" travando no painel. O banco tinha reiniciado sozinho por conta própria da infraestrutura da Supabase (~14h38-14h54, sem relação com código). Investigando a fundo o que sobrou de lento depois disso, achado real e nada óbvio: `pessoas.foto_url` guarda a foto de cada pessoa como texto base64 direto na linha (não como link pra um arquivo à parte) — 174KB de média, **22MB somando as 128 pessoas da Imperatriz** — baixado por inteiro toda vez que a lista de Ritmistas/Diretoria recarrega (a cada 30s sozinho, e logo depois de qualquer Ativar/Suspender/etc, que recarregam a lista pra atualizar a tela). Essa segunda parte é a causa raiz do "Ativar sem resposta": o banco aprovava na hora (confirmado direto no banco, menos de 1s), mas a tela ficava presa baixando 22MB antes de mostrar qualquer coisa — confirmado no histórico que o Diretor chegou a clicar a mesma pessoa 6-7 vezes seguidas achando que travou. Três mitigações aplicadas, sem tocar em nenhum dado de foto: atualização automática e recarregamento pós-ação passam a pular a foto (reaproveitando a já carregada de quem já estava na tela); abrir a tela pela primeira vez mostra a lista na hora (nome/status, já clicável) e completa a foto de cada um logo em seguida, silenciosamente. Conserto definitivo (comprimir as fotos de verdade) fica pra depois, com calma — confirmado que a ferramenta certa (Pillow) já está disponível quando for a hora. No caminho, achado e documentado um padrão a lembrar: `baterias` tem o mesmo gatilho de proteção de edição já conhecido em `pessoas`/`vinculos` — `UPDATE` direto via SQL parece funcionar (sem erro) mas é silenciosamente revertido, precisa desligar o gatilho antes e religar depois. Usado pra corrigir a logo da bateria "Swing da Leopoldina" (tinha uma margem transparente ao redor do círculo que não deixava encaixar certinho na moldura que o TumTu desenha por cima). Fechando a sessão, "Aniversariantes do mês" ganhou totalizador (mesmo padrão de "Ritmistas por Instrumento") e virou acordeão — mas ao contrário dos outros, esse sempre nasce aberto a cada carregamento da tela, nunca lembra que a pessoa fechou da última vez. Detalhe técnico completo de tudo: `tumtu-documentacao-tecnica.md` seção 56.

✅ **Sessão de 26/ago/2026: Repique de Bossa controlado por permissões, campos obrigatórios finalmente publicados, Convidados com Instrumento, card "Menores ativos", dois casos de suporte** — cinco frentes. **Repique de Bossa**: campo delicado, saiu de Meu Perfil/ficha por padrão e passou a depender de permissão explícita (dois interruptores por bateria pro próprio Ritmista ver/marcar; duas capacidades por pessoa pra Diretoria ver/marcar de um Ritmista) — trava real no banco (trigger) e na tela (nunca revela o valor antes de confirmar permissão). Correção de escopo no meio da sessão: o selo/filtro/carteirinha (fora da ficha) continuam públicos como sempre foram, só o campo dentro da ficha ficou restrito — ela esclareceu que a sensibilidade era só sobre esse ponto específico. **Campos obrigatórios**: achado real — a trava de Salvar (Nome/CPF/Endereço etc. em branco) tinha sido construída e testada em 25/ago mas ficou esquecida numa branch de preview nunca publicada; corrigido (mergeada) e acrescentado o asterisco visual (esse sim novo), igual `cadastro.html`. **Convidados**: placeholder de Observações revisado ("convidado da bateria"), e novo campo Instrumento obrigatório quando o Tipo é "Ritmistas". **Visão Geral**: novo card "Menores ativos", lado a lado com o card de Convidados (metade da tela cada). **Dois casos de suporte**: Julio Cesar Santos Silva — conta saudável, só senha esquecida, e-mail de redefinição disparado manualmente por mim (Super Admin nunca vê/define senha de outra pessoa); Caio Costa — CPF que ele citou não tinha nada a ver, causa real era um e-mail com conta de acesso criada mas ficha nunca salva (cadastro trava no meio: cria acesso, DEPOIS salva a ficha, os dois passos não são uma coisa só) — apagada a conta órfã, ele pode recomeçar. Detalhe técnico completo de tudo: `tumtu-documentacao-tecnica.md` seção 57.

🚧 **Pendência criada em 26/ago/2026, a fazer em breve (ela pediu pra registrar e retomar depois — bloqueada nesta sessão por queda da ferramenta MCP do Supabase, banco/site em si continuavam saudáveis)**: mitigar de verdade o problema do cadastro "pela metade" achado no caso do Caio Costa (ver acima) — hoje, se alguém cai exatamente nesse intervalo (acesso criado, ficha não salva) e tenta se cadastrar de novo, cai num erro em inglês sem explicação ("User already registered"), e só um Super Admin consegue destravar manualmente (apagando a conta órfã no banco). **Desenho já combinado com ela**: estender `verificar_pessoa_existente()` pra também detectar essa conta órfã (e-mail existe em `auth.users`, mas nenhuma linha correspondente em `pessoas`); quando detectado, `cadastro.html` tenta `signInWithPassword` com a senha que a pessoa acabou de digitar no formulário — se bater (ela repetiu a mesma senha da primeira tentativa), completa o cadastro sozinho, sem erro nenhum visível; se não bater, mostra mensagem clara em português pedindo "Esqueci minha senha" em vez do erro em inglês. Ela confirmou entender o mecanismo e aprovou ("faz sentido, pode implementar") — falta só implementar quando a ferramenta de banco voltar.

✅ **"Aniversariantes do mês" reformado + Entrega de Figurino reformada (27/ago/2026)** — número do dia no lugar do 🎂 de cada linha em Aniversariantes ("Não quero bolo no lugar do número, deixa o número"), título ganha o nome do mês ("Aniversariantes do mês · Agosto"), quem faz aniversário hoje ganha "🎉 Hoje!" ao lado do nome, "Dia XX" saiu da linha de detalhe (ficaria redundante com o número na frente). **Entrega de Figurino**: marcar como entregue exige confirmação de propósito (toque abre "Confirma?"/Sim/Cancelar, nunca marca com um toque só); depois de marcado, o check trava (✓ verde ao lado do nome) e só desfaz clicando em "Desfazer"; não entregues sempre em cima da lista; novo filtro por status (Todos/Não entregues/Entregues); contador separado por Ritmistas x Convidados quando a peça inclui Convidados; totalizador subiu pro topo da tela (pedido dela depois de testar ao vivo — "tenho que escrolar tudo para ver"); ficha da pessoa (Entrega de Figurinos) ganhou o mesmo padrão visual (rótulo e status na mesma linha, valor "Entregue" em verde — ela pediu ao lado, não embaixo, como era a primeira versão); card do Convidado passou a mostrar quais figurinos ele já recebeu. Tudo desenhado antes via prévia interativa (artifact), aprovado por ela, publicado direto ("Pode publicar."). **Achado real no caminho, não é bug**: ela ficou confusa que "Camisa da Final" só aparecia pra Ritmistas na Entrega — investigado e confirmado que cada peça da biblioteca mestre (Super Admin → Configurações → Figurino) é sempre de UM público só (Ritmista OU Mestre OU Diretor de Bateria OU Diretor); pra outros grupos também terem "Camisa da Final", precisa cadastrar a peça de novo, uma vez por grupo, e ativar em cada bateria — ela viu isso na tela e confirmou que só tinha cadastrado pra Ritmistas mesmo. **Confirmado também**: excluir um Convidado já existe (`excluirExtra`, botão "Excluir" dentro do editor, só aparece editando um já existente) — ela não lembrava, mas já estava implementado desde a criação de Convidados.

🚧 **Pendência grande criada em 27/ago/2026, bloqueada pela queda da ferramenta MCP do Supabase (ela mesma confirmou que não volta ainda hoje) — reforma do Figurino pra Público múltiplo por peça.** Foi direto da confusão acima: ela achou chato ter que cadastrar "Camisa da Final" 4 vezes (uma por grupo) só porque hoje cada peça é de um público só. Desenho combinado com ela: 1) **Biblioteca mestre**: campo "Público" deixa de ser um `<select>` de escolha única e vira checkboxes — uma peça passa a poder cobrir vários grupos ao mesmo tempo (precisa migração no banco: `figurino_itens_mestre.publico`, hoje texto de um valor só, vira lista); 2) **Ativação por bateria**: continua um interruptor só por peça (já é por peça, não por público — não muda a mecânica, só o rótulo passa a mostrar todos os grupos que a peça cobre); 3) **Tela de Entrega, o motivo real do pedido**: abrir uma peça mostra todo mundo dos grupos marcados junto, com título de seção por grupo (Ritmistas/Mestres/Diretores de Bateria/Diretores/Convidados) e um filtro novo por grupo (junto dos que já existem, instrumento e status) — pra ela trocar de filtro em vez de trocar de tela numa entrega de verdade ("fico imaginando ter que ficar trocando toda hora de tela pra entregar camisa"); 4) **Contador do topo**: passa a separar por cada grupo presente na peça (hoje só separa Ritmista/Convidado, pode chegar a 5 blocos). Toca em várias partes do código (`renderizarEditorFigurinoMestre`, `renderizarConfigFigurino`, `renderizarFigurinoLista`, `carregarEntregasFigurino`, `renderizarEntregasFigurinoLista`, `grupoExtraDoPublico`, e a leitura em `fpRenderizarEntregaFigurino` na ficha) — fica pra quando o banco voltar.

🚧 **Pendência grande criada em 26/ago/2026: reforma completa do modelo de Permissões — desenho fechado em conversa longa com ela, NADA implementado ainda (precisa do banco, que ficou fora do ar a sessão toda).** Ela relatou estar achando o menu Permissões cada dia mais confuso e "toda hora precisamos colocar algo novo" — decidimos junto que vale reestruturar antes de deixar crescer mais bagunçado. Sequência do que foi decidido:

- **Levantamento completo** (feito por um agente em paralelo, cobrindo `admin.html`/`ficha-perfil.js`/`carteirinha.html`) confirmou: hoje existem **3 mecanismos de permissão misturados sem distinção** — capacidade por pessoa (`vinculos.capacidades` jsonb), interruptor por bateria (colunas soltas em `baterias`, ex: `ritmista_pode_editar_medidas`), e regra fixa no código sem toggle nenhum (ex: `fpCamposEditaveis` decide por nome de cargo, não por capacidade). Achados de bugs reais no caminho: o botão Editar de Instrumento/Medidas na ficha de um Ritmista aparece pra qualquer Diretoria mesmo sem `editar_ritmistas` — a pessoa edita, salva, parece funcionar, mas o banco reverte em silêncio, sem avisar; Suspender/Desligar/Reativar dentro do modal da ficha não checam capacidade nenhuma (só o Ativar/Rejeitar do card principal checa); "Ver carteirinha ↗" nunca checou capacidade em lugar nenhum; "Naipe que lidera" é visível pra qualquer um com `ver_acessos`, sem controle; CPF/endereço/emergência/responsável são tudo-ou-nada junto com `ver_ritmistas`/`ver_acessos`; editar a ficha de outro Mestre/Diretor/Apoio (não-self) hoje só é possível pro Super Admin, sem capacidade nenhuma pra abrir isso; `editar_configuracoes` existe no catálogo mas não faz nada (morta).
- **Modelo aprovado por ela pra resolver a raiz do problema** ("toda hora precisamos colocar algo novo"): cada permissão passa a ter um **padrão por grupo** (os 4 grupos que ela definiu: Ritmista, Diretor [antigo "apoio"], Diretor de Bateria [antigo "diretor"], Mestre) e, por cima, um **ajuste por pessoa específica** que vence o padrão do grupo quando existir — regra: "se a pessoa tem marcação própria, vale a marcação própria; senão, vale o padrão do grupo dela." Isso elimina o "pessoa por pessoa toda vez" que ela estava sentindo.
- **Princípio-guia que ela fixou no meio da conversa, vale pra toda a reforma**: "Ninguém entra em padrão nenhum, tudo tem que ser configurável. Sempre." — ou seja, mesmo os "padrões por grupo" (ex: Mestre sem restrição por padrão) são só **valores iniciais sugeridos guardados como dado**, nunca uma regra travada direto no código. Isso implica, como consequência natural (ainda **não decidido se entra nesta rodada** — ela pediu pra eu avisar, não pra já resolver): as regras hoje fixas por nome de cargo em `fpCamposEditaveis` (ex: só Diretor edita o próprio Naipe, só mestre/diretor/apoio editam Medidas) também deveriam virar configuráveis. Regra que **fica de fora** desse princípio, intocável: Super Admin sempre vê e faz tudo, em qualquer lugar, sem excecão — isso não é uma permissão que possa ser removida ou esquecida na reforma.
- **Proposta de catálogo novo** (rascunhada por um agente, ela ainda vai revisar/ajustar): separa `aprovar_ritmistas`/`aprovar_acessos` em `aprovar_`/`rejeitar_`/`suspender_`/`desligar_`/`reativar_` (uma pra cada ação, tanto Ritmista quanto Diretoria); separa `editar_ritmistas` em `editar_instrumento_ritmista`/`editar_medidas_ritmista`; cria `ver_naipe`/`editar_naipe` (pedido dela); cria `ver_dados_sensiveis_ritmistas`/`ver_dados_sensiveis_acessos`; cria `editar_acessos` (editar outro Mestre/Diretor/Apoio — recomendado só por pessoa, nunca por grupo inteiro, por ser muito poder); cria `ver_carteirinha_outros`; separa `ver_relatorios` em `exportar_ritmistas`/`exportar_diretoria`; separa `ver_configuracoes` em `editar_instrumentos`/`editar_medidas`/`editar_vagas`/`editar_figurino_bateria`/`editar_toggles_ritmista`; remove `editar_configuracoes` (morta). Detalhe completo (tabelas): `tumtu-documentacao-tecnica.md` seção 58.
- **Item novo adicionado ao catálogo em 26/ago/2026**: marcar/desmarcar a "Declaração do Responsável" (menor de idade) na ficha — hoje `alternarDeclaracao()` em `admin.html` não tem checagem de capacidade nenhuma, qualquer um que abra a ficha consegue clicar. Vira permissão nova (`editar_declaracao_responsavel`, nome sugerido), pra Diretoria em geral — e ela pediu explicitamente que também esteja disponível **pros Diretores de Naipe** (ou seja, essa permissão precisa fazer sentido dentro do escopo restrito ainda em desenho, seção acima — um Diretor de Naipe deveria poder marcar essa declaração pelo menos dos Ritmistas do próprio naipe).
- **Pedido separado, mais pesado, ainda SEM desenho fechado — próxima etapa depois desta**: um Diretor real pediu que Diretor de Naipe só veja/aprove/edite/libere figurino do **próprio naipe**, perdendo a visão do resto — ela mesma reconheceu que "muda até a forma de filtrar na frente" e pediu ajuda pra estruturar. Regras já confirmadas com ela antes de desenhar (importantes, não esquecer): ter naipe atribuído e ficar restrito são **duas coisas separadas** — um Diretor pode liderar um naipe E ter função de administrador, vendo tudo (não é automático); **Mestre**, por padrão, também não fica restrito; a restrição é sempre um ajuste configurável por pessoa, nunca amarrada automaticamente a "tem naipe". Ainda faltam decidir: se a restrição também esconde outros membros da Diretoria (não só Ritmistas fora do naipe) e o que acontece com um Diretor sem naipe nenhum atribuído.
- **Novidade que nasceu no meio dessa conversa (caso de uso real, ela vai perguntar pro Diretor antes de confirmar)**: um Mestre quer usar a gestão mas não quer distribuir carteirinha digital pra Ritmistas esse ano (physical cards já foram pagas, ele acha injusto dar a digital de graça agora) — ela quer importar todos os Ritmistas de uma planilha/Forms de uma vez (bulk import, sem passar pelo autocadastro), com conta de login criada mas **senha aleatória de verdade** (ela cogitou CPF como senha — **recusado**, mesmo motivo já decidido em 22/jul/2026: CPF não é segredo, aparece em vários lugares do próprio sistema, usar como senha anula a proteção). Pra isso, novo par de interruptores por bateria, um por grupo (dentro da mesma reforma acima, não um "Modelo Comercial" à parte): "Ritmistas sem carteirinha esta temporada" e "Diretoria sem carteirinha esta temporada" (podem ligar/desligar independentes; Diretoria pode ter carteirinha mesmo com Ritmistas sem, e vice-versa) — quando ligado, `login.html`/`carteirinha.html` mostram mensagem explicando em vez do cartão, mesmo se a pessoa entrar com a senha certa. Fluxo completo combinado: 1) Diretor pede que Ritmistas preencham um Forms (**precisa ter e-mail de verdade, ela foi avisada**); 2) importação em lote no banco (pessoas+vínculos+login com senha aleatória); 3) Ritmista aparece na gestão do Mestre, sem carteirinha; 4) quando decidirem liberar (temporada seguinte, por exemplo): desliga o interruptor + dispara "esqueci minha senha" em lote pra todo mundo de uma vez. **Confirmado no código (26/ago/2026)**: importar direto no banco já como `status = 'aprovado'` não dispara o e-mail de "cadastro aprovado" (`notificar-aprovacao`) pra ninguém — essa função só é chamada pelo próprio navegador, no exato momento em que alguém clica "Ativar/Aprovar" na tela (`admin.html`, função `notificarAprovacao`, chamada só em `atualizarStatus` e no fluxo de aprovar Diretoria); não existe gatilho nenhum do banco disparando esse e-mail sozinho. Ou seja, a importação em lote pode inserir todo mundo já aprovado sem nenhum ajuste extra de código — ninguém recebe esse e-mail, porque ninguém clicou em nada na tela.
- **Ideia levantada e explicitamente pausada por ela** (não descartada, só posta de lado por enquanto — ela disse "se for um problema sério, vamos deixar de lado, não faremos agora"): tornar configurável até o fato de Ritmista nunca ter acesso a nenhuma parte do painel de gestão hoje — isso é uma mudança bem maior (abre uma porta que hoje não existe em lugar nenhum do código), então **não entra nesta reforma**, só ficou registrado como ideia futura.

---

## Contas de teste

Banco roda só com dados fake (`dados/tumtu-dados-fake-reset.xlsx`). Senha padrão de todas as contas fake: `Teste123`. Super Admin (Márcia): e-mail `tumtuapp@gmail.com`, senha `tumtu2027`.
