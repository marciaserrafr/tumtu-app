# CLAUDE.md

> Este arquivo é lido automaticamente pelo Claude Code toda vez que uma sessão começa nesta pasta. É a ficha de instruções fixas do projeto — diferente dos documentos em `docs/`, que são a documentação de produto/técnica para humanos. Atualizar sempre que uma decisão importante mudar o que está escrito aqui.
> **Regra de tamanho, criada em 31/ago/2026**: este arquivo passou do limite de tamanho que o Claude Code consegue carregar de uma vez, porque o "diário de sessões" (o que virou a seção "Estado atual" antiga) cresceu demais. Esse diário foi movido pra `docs/tumtu-historico-sessoes.md` — aqui fica só um resumo curto do estado atual + pendências abertas. **Daqui pra frente, ao final de cada sessão, registrar o resumo de "o que mudou" no arquivo de histórico (`docs/tumtu-historico-sessoes.md`), não aqui** — este arquivo deve continuar enxuto.
> Última atualização: 06/set/2026, continuação (pendência nº1 do notch resolvida — a causa real era 526KB de JS inline sem `defer`, não CSS; extraído pra `admin-logic-1.js`/`admin-logic-2.js`. Publicado em produção pela primeira vez todo o redesenho "Passaporte" do spinner. Dois bugs urgentes reais corrigidos: Histórico vazio pra quem não é Super Admin, depois vazamento grave misturando baterias de escolas diferentes. Botão "Super Admin" sumido — causa real finalmente achada e confirmada por ela no celular (`overflow:auto` herdado cortando um elemento `fixed` filho, bug conhecido do Safari) + lentidão de clique corrigida (mesmo padrão de feedback imediato). Spinner: mais uma tentativa de ajustar a altura da moldura sem sucesso ("ultrapassou tudo" de novo) — revertida, causa entendida (descompasso de border-radius nos cantos) mas não resolvida; ela pediu pra parar por cansaço. Ver `docs/tumtu-historico-sessoes.md`, sessão de 06/set continuação, pra todo o detalhe)

---

## O que é o TumTu

SaaS de gestão de baterias de escola de samba: cadastro de ritmistas, aprovação por Mestre/Diretor, carteirinha digital. Nome vem do som do surdo de bateria ("TUM-TU, TUM-TU..."). Visão de virar produto comercial multi-escola no futuro.

**Quem constrói:** Márcia Serra — empreendedora, **não é desenvolvedora**, zero conhecimento prévio de programação. Todo o código é escrito pela IA (Claude Code); Márcia decide produto, UX e regras de negócio. Ela está aprendendo sobre IA construindo este projeto ao mesmo tempo.

---

## Antes de mexer em qualquer coisa: mapa da documentação

Este projeto tem documentação de produto detalhada em `docs/` — **leia o documento relevante antes de qualquer mudança estrutural**, não assuma.

| Arquivo | Cobre | Quando ler |
|---|---|---|
| `docs/tumtu-visao-geral.md` | Visão de negócio, marca, identidade visual, jurídico/LGPD | Antes de decisões de produto/negócio |
| `docs/tumtu-mvp.md` | Escopo funcional do MVP, perfis, fluxos, regras numeradas (1 a 11) | Antes de mudar fluxo de cadastro/aprovação/permissão |
| `docs/tumtu-design-guide.md` | Paleta, tipografia, componentes visuais, checklist obrigatório antes de mudança visual | **Sempre antes de qualquer alteração visual** |
| `docs/tumtu-documentacao-tecnica.md` | Arquitetura, modelo de dados, RLS, Edge Function, histórico de decisões técnicas | Antes de mexer em banco, autenticação, permissões |
| `docs/tumtu-plano-de-testes.md` | Estratégia de teste, dados fake, roteiros de teste manual | Antes de testar mudanças ou popular dados |
| `docs/tumtu-estrategia-piloto.md` | Estratégia de entrada via carteirinha beta gratuita, operação do piloto (problema de ritmista em múltiplas baterias já resolvido em 13/jul/2026, ver seção 14) | Antes de mexer em cadastro/aprovação/login pensando no piloto |
| `docs/tumtu-historico-sessoes.md` | Diário completo de tudo que já foi feito, sessão por sessão, do mais antigo pro mais recente | Se precisar do detalhe completo de uma decisão antiga que não está no resumo curto deste arquivo |

`docs/README.md` **não existe mais** — foi removido em 09/jul/2026 por descrever uma versão bem antiga do sistema (senha em texto puro, sem Super Admin) que divergia dos docs acima.

---

## Regras que eu nunca posso quebrar

### Comportamento geral
- **Nunca mudar layout, espaçamento ou organização visual sem aprovação explícita.** Se a mudança não foi pedida, não faço. Quando há dúvida sobre estilo, nome de botão, cor ou layout, mostro opções e espero a Márcia escolher antes de implementar.
- **Nunca remover/desistir de algo que ela pediu, mesmo depois de várias tentativas sem sucesso, sem perguntar antes.** Regra criada em 22/ago/2026, depois de eu remover sozinho o rótulo "Nome usado" (Configurações → Instrumentos) porque não conseguia acertar o alinhamento dele — ela tinha pedido pra AJUSTAR, não pra tirar, e ficou justificadamente brava ("QUEM MANDOU VOCÊ TIRAR?"). Se depois de várias tentativas eu não estiver conseguindo acertar algo visual, o caminho certo é falar isso pra ela e perguntar como seguir (tentar de outro jeito, pausar, ou só aí sim remover) — nunca decidir sozinho que "o mais simples é tirar" e simplesmente fazer.
- **Nunca adicionar detalhe visual não pedido** (separadores, ícones extras, cores, espaçamentos) — implementar exatamente o que foi descrito, nada a mais.
- **Confirmar antes de subir ao GitHub.** Regra reforçada em 18/jul/2026 (a Márcia pediu essa "rede de segurança" explicitamente, depois de perceber que hoje toda mudança vai direto pra produção sem ambiente de teste separado): antes de publicar de verdade (`git push` na `main`, que a Vercel publica automaticamente em `tumtu.com.br`), gerar um **link de teste avulso** e mandar pra ela conferir no próprio celular/computador — não só descrever em palavras ou mostrar print estático. Só depois da aprovação dela, commit + push de verdade (ou merge do branch de preview pra `main`). Vale pra qualquer mudança visual ou de fluxo perceptível por quem usa o app — não precisa pra ajustes 100% internos (ex: só comentário de código, só reorganização de memória). **Método atualizado em 14/ago/2026** (o `vercel` CLI não está disponível neste ambiente — ver "Stack e infraestrutura"): commitar num branch dedicado (`preview/nome-da-mudanca`, nunca direto na `main`) → `git push` desse branch → a integração GitHub↔Vercel do projeto `tumtu-app` publica sozinha um preview pra esse branch, sem comando manual → pegar a URL estável desse preview (formato `tumtu-app-git-<branch>-marcia-ritmistas.vercel.app`, via ferramenta MCP `list_deployments`) → gerar um link sem exigência de login da Vercel com a ferramenta MCP `get_access_to_vercel_url` (expira em ~23h) e mandar pra ela abrir **direto no navegador** (não de dentro do WhatsApp/Instagram — o navegador embutido desses apps pode bloquear o cookie que esse link depende, já aconteceu de "funcionar" do lado de cá e não abrir pra ela). **Exceção pontual, só quando ela pedir nesses termos explicitamente** (aconteceu em 31/ago/2026, pra "Confirmar Presença": "acho mais fácil, ngm sabe dessa funcionalidade e eu controlo aqui") — publicar direto na `main`, pulando o link de teste. Detalhe técnico completo: `tumtu-documentacao-tecnica.md` seção 32.
- **Quando a Márcia disser "estava melhor antes"** — reverter imediatamente, sem questionar e sem tentar "melhorar" a versão anterior por conta própria.
- **Dimensão da carteirinha é fixa em 300×540px (documentado em `docs/tumtu-design-guide.md`, seção 7.3) — nunca aumentar o tamanho do cartão pra caber conteúdo novo.** Se fonte, logo, respiro ou qualquer elemento não couber, o ajuste é no conteúdo (reduzir/redistribuir), nunca no tamanho do cartão. Mudar essa dimensão exige aprovação explícita da Márcia todas as vezes — regra criada em 13/jul/2026 depois de eu aumentar o tamanho sem avisar duas vezes seguidas na mesma sessão. (Único ajuste aprovado desde então: 500px→540px de altura em 14/jul/2026, a pedido explícito dela — "um pouquinho mais comprida"; a regra de nunca mudar sem aprovar de novo continua valendo.)
- **Toda tela que carrega dados tem que aparecer já montada por completo — nunca revelar título/painel primeiro e o conteúdo (lista, cards, números) depois.** O jeito certo: buscar os dados **antes** de trocar qualquer coisa visível na tela (trocar de aba, abrir uma sub-tela), e só então revelar tudo de uma vez — sem spinner dentro de um container, sem overlay de tela cheia cobrindo o resto, nenhuma etapa intermediária visível. Regra criada em 01/set/2026 depois de eu cometer o mesmo erro 3 vezes seguidas na mesma sessão até ela apontar a solução certa (spinner pequeno dentro da lista → rejeitado: "isso ainda conta como revelar pela metade"; overlay escuro de tela cheia → também rejeitado: "não entendi pq a necessidade do spinner com tela preta... isso tá muito ruim"). Ela já tinha relatado esse mesmo problema em sessões anteriores. **Exceção aceita, não é uma violação da regra:** em Ritmistas/Diretoria/Convidados, a FOTO de cada pessoa pode chegar em segundo plano *depois* da tela já aparecer completa (card nasce com a inicial no lugar da foto, e ela substitui silenciosamente quando chega, sem spinner nenhum) — o card em si já nasce com toda informação que importa, só a imagem "acabamento" é que chega depois. Esse é o modelo de referência pra qualquer tela nova que precise mostrar foto de gente.

### Regras de produto (não violar sem confirmar com a Márcia)
- **Um único formulário de cadastro** (`cadastro.html`, renomeado de `index.html` em 12/jul/2026), reaproveitado em 3 modos (público/link fixo/manual) — nunca duplicar em telas separadas. O modo "público" (escolher escola livremente) existe no código mas não é linkado de lugar nenhum hoje — todo cadastro real acontece via link fixo, vinculado a uma bateria específica.
- **Um único motor de edição de perfil** (`ficha-perfil.js` + `ficha-perfil.partial.html`) compartilhado por `admin.html` e `carteirinha.html` — nunca copiar/colar essa lógica numa tela nova. (`super-admin.html` foi apagado em 19/ago/2026 — `admin.html` unificou as duas telas.)
- **Telas que mostram o mesmo modelo de dados para perfis diferentes devem ter os mesmos campos.** Ex: modal de Admin e modal de Ritmista mostram os mesmos campos — a diferença entre perfis é sobre quem pode ver/editar, não sobre quais campos existem na tela.
- **Dados de escola são sempre variáveis via `config-escola.js`** — nunca hardcoded no código.
- **Conta de Super Admin nunca é exposta a exclusão pela interface**, em nenhuma tela — evita lockout, já que ninguém acima dela pode restaurar o acesso.
- **Super Admin nunca tem acesso a ver/definir a senha de outra pessoa** — decisão ética explícita da Márcia. Reset de senha é sempre self-service via Supabase Auth nativo.
- **Toda feature nova que toque em Diretoria deve tratar Mestre/Diretor de Bateria/Diretor (Apoio) como campos ou checkboxes SEPARADOS, nunca um bloco único "Inclui Diretoria".** Regra fixada em 29/ago/2026 depois dela rejeitar exatamente esse desenho no módulo de Presença ("o mestre pode não querer dar presença, mas quer presença só do apoio... não dá pra prever a combinação").

### Priorização
- **Função antes de polimento.** Ao sugerir o que atacar a seguir, separar "não funciona / está quebrado" de "funciona mas podia ser mais conveniente/bonito" — na dúvida, priorizar o primeiro grupo. Não presumir que uma melhoria de UX bem definida é prioridade só por estar pronta pra implementar.
- **O card do ritmista é o elemento mais importante da tela do Admin.** Tudo ao redor (filtros, vagas, seções extras) é secundário — deve ficar clean, sem poluir visualmente o card.

### Hierarquia visual
- Botão de ação principal (ex: "Cadastro") precisa de destaque real: fundo sólido escuro `#12101a` ou dourado `#D4AF37` (tokens `--cor-fundo-escuro`/`--cor-destaque` em `styles/tokens.css`). Botões secundários = só borda, transparente.
- Seções auxiliares (ex: "Vagas por Instrumento") devem ser simples — listas com texto, sem chips/cards aninhados/cores excessivas.

### Segurança
- **Auditar segurança sozinho, a cada sessão com mudança de banco — sem esperar ela pedir.** Regra endurecida em 28/ago/2026 depois dela ter que pedir de novo e a checagem achar um vazamento crítico ativo. Rodar a ferramenta automática de segurança do Supabase **primeiro** (pega sistematicamente o que checagem manual não cobre), depois checagem manual direcionada nas áreas mexidas. Testar com chamada HTTP real (chave pública bloqueada) e com login de verdade (edição legítima continua funcionando) — não só ler o código.

---

## Como trabalhar com a Márcia

- Explicar tudo do zero absoluto — o que é cada ferramenta, por que está sendo usada, cada passo em linguagem simples. Nunca assumir conhecimento técnico prévio, inclusive sobre onde clicar em painéis externos (Supabase, GitHub, Vercel).
- Ela fica ansiosa com telas desconhecidas (painel de controle de versão, opções administrativas do Supabase) — tranquilizar primeiro ("nada quebrou, nada foi feito errado") antes de explicar o próximo passo.
- Ela está aos poucos aprendendo a se organizar (pastas, versionamento) — reforçar positivamente esses avanços, sem forçar demais de uma vez.

---

## Stack e infraestrutura

- **Frontend:** HTML + CSS + JavaScript puro. Sem framework, sem bundler, sem `package.json`. Cada tela é um `.html` autocontido.
- **Backend:** Supabase (Postgres + Auth + Edge Functions), sem servidor próprio. Project ref `pkvzsgrkylrkyzligeim`.
- **E-mail transacional (e-mails automáticos do Supabase Auth — redefinir senha, etc.):** servidor de envio próprio via **Resend** (custom SMTP, configurado em Authentication → Emails → SMTP Settings do Supabase, 14/ago/2026) — sem isso, o Supabase usa um servidor genérico deles e trava a edição do texto dos e-mails, que ficam em inglês. Conta do Resend é a pessoal da Márcia (`marciaserrafr@gmail.com`, mesmo padrão de GitHub/Vercel/Supabase — `tumtuapp@gmail.com` fica só pro papel de Super Admin dentro do próprio app). Domínio `tumtu.com.br` verificado no Resend via registros DNS na zona do **Registro.br** (não é a Vercel que hospeda o DNS). Remetente reaproveita o `suporte@tumtu.com.br` já existente. **Só o template "Reset password" foi traduzido até agora** — os outros e-mails do Supabase (confirmação de cadastro, etc.) continuam em inglês, sem confirmação ainda se algum deles é realmente enviado no fluxo atual do app.
- **Deploy:** GitHub (`marciaserrafr/tumtu-app`) → Vercel (projeto **`tumtu-app`**, team `marcia-ritmistas` — é o projeto com a integração GitHub de verdade, publica automático a cada `git push`, tanto na `main` quanto em qualquer outro branch, como preview). **Existe um projeto Vercel antigo separado, `ritmistas-app`, que NÃO é esse** — tem vários deploys avulsos de teste sem uso, criados por engano numa sessão anterior; não usar.
- **Domínios no ar**: `tumtu.com.br` (raiz redireciona pra `/login`, ainda sem landing page de divulgação de verdade) e `app.tumtu.com.br` (adicionado 30/ago/2026, mesmo app/deploy, pensado como endereço fixo pra divulgar pra ritmistas instalarem — não substitui o domínio antigo).
- **Ferramentas de linha de comando disponíveis localmente:** `gh` (GitHub CLI, autenticado) — usar em vez de pedir pra Márcia navegar em painéis desconhecidos, sempre que a tarefa permitir. **O `vercel` CLI NÃO está disponível neste ambiente**. Pra qualquer necessidade de deploy/preview/consulta à Vercel, usar as ferramentas MCP (`mcp__claude_ai_Vercel__*`: `list_deployments`, `get_access_to_vercel_url`, `get_project_deployment_protection`, etc.) — ver fluxo completo na regra "Confirmar antes de subir ao GitHub" acima. **Nota**: em pelo menos uma sessão (31/ago/2026) nem `gh` nem as ferramentas MCP da Vercel estavam disponíveis — se isso se repetir, contornar consultando a API pública do GitHub via `curl` (o repo é público) e sinalizar pra ela como limitação da sessão, não presumir que sumiu de vez.
- **Fonte:** Plus Jakarta Sans (Google Fonts).
- **Biblioteca de terceiros:** `@supabase/supabase-js@2` via CDN; `jsQR` via CDN (leitor de QR, usado no scanner da Diretoria e no "Confirmar Presença" da carteirinha). (`bcryptjs` foi removido em 05/jul/2026 — não usar mais, autenticação é 100% Supabase Auth.)
- **PWA:** instalável direto do navegador (`manifest.json` + `sw.js`), sem loja/custo. **Atenção:** todo arquivo do "app shell" (listado em `APP_SHELL` dentro de `sw.js`) é servido do cache — ao alterar qualquer `.html`/`.css`/`.js` do shell, subir a versão de `CACHE_NAME` em `sw.js`, senão quem já visitou o site continua vendo a versão antiga.
- **Cache-busting por versão na URL (`?v=N`):** `styles/tokens.css`, `styles/components.css`, `carteirinha-tumtu.css`, `ficha-perfil.js`, `ficha-perfil.partial.html` e `config-escola.js` são referenciados com `?v=N` em todo HTML que os usa, e no `fetch()` dentro de `ficha-perfil.js`. **Toda vez que algum desses arquivos mudar, subir esse número em TODOS os lugares que o referenciam** (busca por `?v=N` no projeto) — isso é mais forte que só trocar o `CACHE_NAME`: derrota até cache de operadora/proxy no meio do caminho, porque a URL vira literalmente outro arquivo.
- **`viewport-fit=cover` é incompatível com a barra de baixo fixa deste app** (achado definitivo, 03/set/2026, ver pendência grave "Faixa branca do notch" mais abaixo) — não ligar de novo sem antes reconstruir essa barra.

## Estrutura de pastas

```
Tumtu/
├── *.html, *.css, *.js        # código do app, solto na raiz
├── styles/                    # tokens.css (CSS variables) + components.css (componentes reutilizáveis)
├── icons/                     # ícones do PWA
├── docs/                      # documentação de produto/técnica (.md)
├── dados/                     # planilha de dados fake para popular o banco de testes
├── imagens/                   # material de referência visual — fora do Git (.gitignore)
└── .claude/
```

## Modelo de dados (resumo — detalhe completo em `docs/tumtu-documentacao-tecnica.md`, seção 22)

- **Desde 13/jul/2026, "pessoa" e "vínculo com bateria" são duas tabelas separadas** — ler a seção 22 da documentação técnica antes de mexer em cadastro/login/aprovação/edição de perfil:
  - **`pessoas`**: quem é a pessoa, não muda entre baterias (nome, CPF, endereço, contato de emergência, foto, `super_admin` boolean, `genero`, dados do responsável de menor de idade, `qr_token`). IDs `bigint`, não UUID.
  - **`vinculos`**: o vínculo de uma pessoa com UMA bateria específica (`perfil`, `status`, instrumento, os 4 tamanhos de roupa/Medidas, `aprovado_por`, `capacidades` jsonb de permissão, `naipe`, `repique_bossa`, `nao_desfila`, `observacoes`). Uma pessoa pode ter vários vínculos, um por bateria.
  - **Convenção importante:** `.id` em card/lista/URL (`carteirinha.html?id=`) significa **vínculo**, não pessoa. Só autoedição e `aprovado_por` usam `pessoa_id` de verdade.
  - A tabela antiga `ritmistas` (que juntava as duas coisas numa linha só) ainda existe no banco como rede de segurança, sem receber mais escrita. **Decisão registrada em 27/ago/2026: NÃO apagar** — ela decidiu manter mesmo com risco baixíssimo confirmado ("ainda me ajuda com testes"). Não sugerir apagar de novo sem ela puxar o assunto.
- `cargo` (o que aparece na carteirinha) é separado de permissão de acesso. Permissões granulares vivem em `vinculos.capacidades` (jsonb, uma capacidade por chave, atribuída direto por pessoa — reforma de permissões concluída em 28/ago/2026, ver `tumtu-documentacao-tecnica.md` seção 62). Os campos antigos `vinculos.nivel_acesso`/`niveis_acesso`/`escola_niveis_acesso` (perfil nomeado) ficaram obsoletos.
- `vinculos.eh_admin_bateria` (boolean, 04/set/2026): marca um Diretor de Bateria como "Admin da Bateria" — atributo de ficha (como Naipe), não uma capacidade solta. Tabela `permissoes_padrao` (cargo → capacidades jsonb, 5 linhas fixas: mestre/diretor_admin/diretor_naipe/diretor/apoio) guarda o molde global aplicado automaticamente na 1ª aprovação de cada pessoa (trigger `trg_permissoes_padrao_vinculos`). **Upgrade automático depois de já aprovado** (mesmo dia, sessão seguinte): quando `eh_admin_bateria` vira `true` ou `naipe` é preenchido pela 1ª vez (e a pessoa ainda não é Admin — Admin sempre manda mais), triggers próprias (`trg_permissoes_padrao_admin_vinculos`/`trg_permissoes_padrao_naipe_vinculos`) aplicam o molde correspondente **por cima** do que a pessoa já tinha, via `jsonb_capacidades_merge()` — nunca desliga uma capacidade já ligada, só liga o que falta (Diretor de Naipe comum que vira Admin só ganha, nunca perde).
- Tabelas `escolas` e `baterias` completam o modelo. `baterias.modo_piloto` (rótulo na tela: **"Modo Carteirinha"**) controla se Mestre/Diretor daquela bateria caem na carteirinha (true) ou no painel admin (false) ao logar.
- Autenticação real via Supabase Auth (`auth.users` ligado por `pessoas.auth_user_id`) — RLS ligado em `pessoas`, `vinculos`, `escolas`, `baterias`. Restrição por coluna (quem edita o quê) é feita por trigger (`aplicar_matriz_edicao_pessoas` + `aplicar_matriz_edicao_vinculos`), não pela policy de RLS. **Funções de RLS corretas pra "minha bateria":** `meu_pessoa_id()` e `minhas_baterias_admin()`. **Nunca usar** `meu_perfil()`/`meu_bateria_id()`/`meu_status()` (funções antigas, congeladas, causam permissão silenciosamente quebrada).
- Instrumentos são configuráveis (biblioteca mestre de categorias/nomenclaturas + ativação por bateria), não uma lista fixa no código — tabelas `instrumento_categorias`, `instrumento_nomenclaturas`, `bateria_instrumentos`.
- **Medidas (camisa/fantasia/calça/sapato e qualquer tipo novo) são um sistema totalmente aberto desde 23/ago/2026** — biblioteca mestre em `medida_tipos` (tipo + escala de tamanhos, todo tipo novo já nasce com escala obrigatória) + ativação por bateria em `bateria_medidas`/`bateria_medida_tipos`.
- **Figurino** (peça de evento, ex: "Camisa da Final") tem lista mestre própria (`figurino_itens_mestre`, Categoria = tipo de peça reaproveitando Medida, `publico` = lista de perfis que a peça cobre) + ativação por bateria (`bateria_figurino_itens`) + registro de entrega (`figurino_entregas`).
- **Eventos/Presença** (módulo novo, 29/ago/2026, gate comercial `baterias.modulo_presenca_ativo`): `evento_tipos` (biblioteca mestre) + `eventos` (por bateria, com `temporada_id` obrigatório e `perfis_diretoria_inclusos` array) + `evento_presencas` (uma linha = presença marcada; sem linha = ausente).
- Vocabulário: o valor "ativo" no banco é literalmente `status = "aprovado"`, não `"ativo"`.

## Estado atual (resumo — histórico completo sessão por sessão em `docs/tumtu-historico-sessoes.md`)

✅ **O que já está em produção, funcionando de ponta a ponta**: autenticação real + RLS, motor único de edição de perfil, cadastro em etapas com autocadastro/link fixo/manual, carteirinha digital com cor/logo dinâmicos por escola, aprovação de Ritmistas/Diretoria com histórico de auditoria, permissões granulares por pessoa (Reforma de Permissões, 28/ago/2026), Instrumentos/Medidas/Figurino como bibliotecas mestre 100% configuráveis (Público/Incluir Convidados do Figurino são decisão por bateria desde 31/ago/2026), "Convidado Especial" — gente de fora da bateria oficial com carteirinha de verdade, fila e permissões próprias (31/ago/2026), exportação para Excel, PWA instalável, Face ID/Digital como trava local de privacidade dentro do app, módulo de Presença via QR Code (leitor embutido no app, tanto pra Diretoria escanear quanto pra Ritmista "Confirmar Presença" sozinho), domínios `tumtu.com.br` e `app.tumtu.com.br` no ar, fundamentos de LGPD (política de privacidade, exclusão de dados sob pedido). Painel de gestão é um único arquivo (`admin.html`) com barra própria pro Super Admin. Auditorias de segurança recorrentes já corrigiram vazamentos reais várias vezes — ver regra "Segurança" acima.

✅ **"Convidado Especial" publicado em produção (31/ago/2026, sessão seguinte, commit `b06eb8a`)** — a causa real do bug de duplicação/perda de dado da sessão anterior (só a última pessoa cadastrada sobrevivia na fila) **não era o banco**: `cadastro.html` e `admin.html` compartilhavam a mesma sessão de login persistida no navegador, e cada autocadastro novo (`signUp()`) sobrescrevia silenciosamente a sessão de quem já estava logado (ex: o Admin, testando no mesmo aparelho) — daí o painel passar a "ver" só a própria ficha da última pessoa criada. Corrigido isolando a sessão do autocadastro num cliente Supabase próprio (`sbCadastro`, nunca persiste em localStorage), que nunca mais troca a sessão de quem já estava logado. Publicado sem ela testar ao vivo antes — decisão dela, explicando o motivo: um Diretor pedindo carteirinha fake pra esposas de Diretores (fora da bateria oficial) estava criando trabalho manual recorrente pra ela gerenciar na mão; preferiu absorver o risco pontual e tirar esse peso de cima. Detalhe técnico completo: `docs/tumtu-historico-sessoes.md` (última entrada) e memória de longo prazo `project_convidado_especial_bug_duplicacao`.

✅ **Figurino: "Público" e "Incluir Convidados" viram decisão por bateria (31/ago/2026, mesma sessão, commits `a84800f` e `e93f78c`).** Antes moravam no item GLOBAL (Super Admin → Configurações → Figurino), valendo pra todas as escolas ao mesmo tempo — ela apontou que só cada bateria sabe quem recebe uma peça (ex: "Camisa da Final") e se quer incluir Convidados nela. Migrado pra `bateria_figurino_itens`, mesmo padrão já usado em Categoria de Figurino/Medida (com backfill dos registros existentes, zero mudança de comportamento pra quem já estava configurado, e correção de uma política de RLS que ainda dependia do campo global). Rótulo do checkbox virou "Convidados" (chegou a virar "Convidado", singular, e voltou pro plural na sessão seguinte). Detalhe técnico: `docs/tumtu-historico-sessoes.md` e memória `project_figurino_publico_por_bateria`.

✅ **Achado real: Figurino/Presença nunca separavam Convidado Especial da conta normal, corrigido e publicado (31/ago/2026, sessão seguinte, commit `36ebb93`).** "Incluir Convidados" em Figurino/Presença sempre buscava a tabela antiga do Convidado Simples (vazia pra quem usa Convidado Especial) — nunca checava `modoConvidadosEspecial()`, o mesmo interruptor por bateria que outras telas já respeitam. Efeito colateral: o Convidado Especial de verdade ficava contado escondido dentro de Ritmistas/Diretoria, sem filtro `eh_convidado`, sem etiqueta nenhuma. Corrigido em Entrega de Figurino, resumo de Figurino na Visão Geral, lista de Presença e marcação de presença/entrega — todos atrás do mesmo gate, gravando sempre em `vinculo_id` (nunca tabela nova) quando é Convidado Especial. Baterias que ainda usam Convidado Simples não mudaram. Detalhe técnico: `docs/tumtu-historico-sessoes.md`.

✅ **Tela de Convidados ganha o padrão visual de Ritmistas/Diretoria (31/ago/2026, mesma sessão, commit `ed6071d`).** Exportar, "Novo cadastro" recolhido, busca, filtro por Tipo de Convidado e por Status, título/contador por tipo na lista — tudo reaproveitando padrões já existentes no app. Detalhe técnico: `docs/tumtu-historico-sessoes.md`.

✅ **Bug do botão "Presença" sem evento ativo, corrigido e publicado (01/set/2026, commit `b2278ea`).** Carteirinha e a confirmação por QR (embutida e `presenca.html`) checavam só se existia algum evento cadastrado na bateria, sem olhar os interruptores Iniciado/Finalizado — corrigido pra usar o mesmo critério de "evento ativo" já usado no card da Visão Geral. Detalhe técnico: `docs/tumtu-historico-sessoes.md`.

✅ **Dashboard do Super Admin redesenhado, publicado (01/set/2026, sessão seguinte, commit `7e79690`).** Sem colunas assimétricas (tudo em faixas de largura total, cartões lado a lado sempre do mesmo tamanho — duas rodadas de rejeição por desalinhamento antes de acertar); novo KPI "Total no cadastro" (qualquer status, não só ativos); "Precisa de você" agora só considera Mestre pendente; nova seção "🎂 Aniversariantes de hoje e amanhã" (todas as baterias juntas); cada card de bateria ganhou logo, detalhamento por categoria (Ritmistas/Diretoria/Convidados) e status secundário, e selos de Evento/Figurino aberto; nova seção "Atividade recente" (todas as baterias); clique no card agora abre direto na Visão Geral (antes ia pra Acessos). Detalhe técnico completo: `docs/tumtu-historico-sessoes.md` (última entrada).

✅ **Telas "montando aos poucos" corrigidas de vez, publicado (01/set/2026, mesma sessão, commit final `d0382f7`).** Regra nova, permanente, registrada acima em "Comportamento geral": nenhuma tela pode revelar título/painel antes do conteúdo — buscar tudo primeiro, revelar de uma vez, nunca spinner nem overlay. Levou 4 tentativas até acertar (spinner interno → rejeitado; overlay de tela cheia → rejeitado; reordenar dentro da função da aba → ainda errado, porque quem revelava o painel primeiro era a função central `trocarAba()`, não a função da aba em si). Aplicado em Convidados (agora espelha `carregarDiretoria()` de verdade), Figurino, Presença, Histórico e toda a troca de aba do Super Admin. Detalhe técnico completo: `docs/tumtu-historico-sessoes.md`.

✅ **Bug real: "Marcar presença dos eventos" (Permissões) ficava travado sem motivo, corrigido e publicado (01/set/2026, mesma sessão, commit `1586cf9`).** Dependia de `ver_eventos`, capacidade de Configurações (catálogo de tipos de evento) sem relação nenhuma com marcar presença — travava o checkbox pra qualquer Diretor sem acesso a Configurações. Virou capacidade solta.

✅ **Permissões reorganizada no modelo "Meu Perfil" (título-pai com tracinho + sub-blocos), publicado em todas as personas (01/set/2026, mesma sessão, commit `56c5f46`).** Ela pediu: título "Meu Perfil" dividido em sub-blocos curtos (Medidas/Instrumento/Desfile/Declaração do Responsável/Observações), sem parágrafo explicativo, rótulo direto ("a ação já diz o que será permitido"). Confundi a área na 1ª tentativa (mexi na permissão por pessoa de Diretoria, que controla o que Mestre/Diretor/Apoio podem fazer COM ritmistas — revertido) em vez da área certa (interruptores de bateria inteira que controlam o que o RITMISTA vê/edita no próprio perfil) — ela teve que corrigir gritando "RITMISTAS!!!!!!!". Título-pai passou por 3 versões (terracota → rejeitado, "já significa ação destrutiva em todo o app"; cópia literal do estilo de rótulo que ela mesma sugeriu → rejeitado, "fica na mesmice"; versão final: texto normal sem caixa-alta + tracinho dourado, com o porquê explicado) até ela aceitar. Depois pediu pra aplicar o MESMO modelo em todo o resto de Permissões — feito: tela de Convidados (interruptor de bateria) e os 8 títulos de grupo do editor por pessoa (Mestre/Diretor/Naipe/Apoio compartilham o mesmo editor). **Regra nova registrada em memória de longo prazo** (`feedback_sempre_sugerir_com_justificativa`): quando eu achar que existe opção melhor que a que ela descreveu, tenho que sugerir por conta própria com o motivo, nunca só executar literal ou aceitar sem contestar — ela foi explícita: "sugira e me explique o pq da escolha, pq o motivo pode ser decisivo pra eu aceitar". **Ela avisou que vai continuar ajustando pessoa por pessoa** — assunto não fechado, retomar se ela trouxer de novo. **Rodadas seguintes já publicadas, mesma sessão:** título "Acesso ao TumTu" (era "Acesso ao painel"); fonte dos checkboxes unificada (13px, sem negrito — peso 700 tinha ficado "pesado" pra ela); "Ficha da Diretoria" também virou grupo próprio "Perfil do Diretor" (mesmo problema de excesso de itens, sugestão própria); "Dados sensíveis" → "Dados Pessoais"; Declaração do Responsável sem "(menores)"; "Visualizar lista" sem o parêntese de campos; novo grupo **"Meu Perfil"** no topo (hoje só com "Editar a própria Medida" — pedido dela: "o mestre e diretor poderá fazer coisas no seu próprio perfil, essa seção não tem"). Detalhe completo: `docs/tumtu-historico-sessoes.md`.

✅ **Permissões: mapeamento completo de "Perfil do Ritmista"/"Perfil do Diretor" + novo "Perfil do Convidado", publicado (01/set/2026, mesma sessão, commit `fff089a`).** Em vez do desenho que tinha sinalizado, ela listou de próprio punho todas as seções que quer mapeadas — "coloque tudo, independente de ter funcionalidade ou não criada... pra mapear todo o sistema, pois senão eu fico maluca toda hora criando permissão aleatória." Todo item sem capacidade real vira `{ semFuncionalidade: true, nota: '...' }` (sem checkbox, só notinha em itálico explicando o motivo), filtrado de `TODAS_CAPACIDADES` pra nunca gravar chave "undefined" no banco. Corrigido também um engano meu: as notas de "Entrega de Figurinos"/"Eventos" confundiam o resumo somente-leitura dentro da ficha da pessoa (`fpRenderizarEntregaFigurino`/`fpRenderizarEventos` em `ficha-perfil.js`, sem gate hoje) com o menu "Figurino"/"Lista de Presença" — ela cortou: "Vc está errada. Uma coisa é Lista de Presença - MENU. Outra coisa é VISUALIZAR o item... dentro de ritmista." Por fim, estendido a Convidados: "Essa ficha do perfil, meu perfil vai servir para TODAS AS PERSONAS, Tanto ritmista, diretores, mestres, convidados, TODOS" — novo grupo "Perfil do Convidado" (14 sub-blocos, só "Instrumento" com capacidade real hoje: `editar_convidados_especiais`, movido pra lá do grupo "Convidados"). **Frente de Permissões segue aberta** — ela vai continuar lapidando pessoa-por-pessoa/grupo-por-grupo nas próximas sessões. Detalhe completo: `docs/tumtu-historico-sessoes.md`.

✅ **Permissões: Endereço e Contato de Emergência separados de Dados Pessoais, + setinha colapsável em cada título-pai, publicados (02/set/2026, commits `4225d39` e `e958893`).** A capacidade única que juntava CPF/documento/endereço/contato de emergência/dados do responsável virou 3 (`ver_dados_sensiveis_*` agora só CPF/documento/responsável, + `ver_endereco_*` e `ver_contato_emergencia_*` novas) — em Perfil do Ritmista E Perfil do Diretor. Confirmado nessa investigação: **editar** endereço não existe pra Diretoria hoje (só autoedição/Super Admin) porque a trigger `aplicar_matriz_edicao_pessoas` não tem a granularidade por capacidade que a trigger de `vinculos` já tem — só **ver** é capacidade jsonb pura, sem mexer em banco. Depois, ela pediu a setinha: "Permissões está ficando nem extenso... uma setinha em cada título Pai... não queria no formato de box, algo mais simples, mais sutil" — implementado nos 4 títulos-pai do app (Acesso ao TumTu, grupos do editor por pessoa, os dois "Meu Perfil"), grupos nascem recolhidos, clique gira a seta. **Backfill resolvido (mesmo dia, sessão seguinte):** a ferramenta de banco voltou a ficar disponível — rodado direto (sem precisar dela mexer no Supabase), conferido 12/12 vínculos de Ritmista e 10/10 de Diretoria migrados, ninguém perdeu visibilidade que já tinha. Detalhe completo: `docs/tumtu-historico-sessoes.md` (última entrada).

✅ **Permissão de Convidados genérica — CONFIRMADO completo (banco + código), 03/set/2026.** Checado nesta sessão: branch `preview/convidados-permissao-generica` (commit `402ea0f`) já está mergeado na `main` (commit `d14fc7b`, 01/set); coluna `baterias.convidado_pode_editar_medida` existe no banco e já está protegida no trigger `aplicar_matriz_edicao_baterias()`. Pendência encerrada — a entrada anterior deste arquivo estava desatualizada.

✅ **E-mails de Rejeitado e Reativado — publicados em produção (04/set/2026, commit `677769e`).** Textos aprovados por ela (ver `docs/tumtu-historico-sessoes.md`). Edge Function `notificar-aprovacao` ganhou parâmetro `tipo` ('aprovado' padrão | 'reativado' | 'rejeitado'); corrigida no caminho a checagem de permissão (Ritmistas/Diretoria têm capacidade separada por ação — aprovar_*/rejeitar_*/reativar_* —, Convidado Especial usa uma única `aprovar_convidados_especiais`). Suspenso/Desligado continuam sem e-mail, de propósito. **Achado extra no caminho**: Super Admin não conseguia editar dados de pessoa Pendente/Rejeitada/Desligada — não era bug de permissão (client nem trigger bloqueavam), era uma regra antiga que escondia o botão Editar via DOM direto sem exceção pro Super Admin, em 4 lugares (Ritmistas/Diretoria/Convidados×2). Corrigido junto.

✅ **Bug grave corrigido: "Copiar de"/"Aplicar a todos" ignorava Naipe/Admin (04/set/2026, commit `97cfda7`).** Achado dela ao vivo, com dano real: aplicou permissão a partir da ficha de um Diretor de Naipe pra "todos os Diretores de Bateria" e o sistema espalhou a permissão operacional dele pra Diretores comuns que não deveriam ter isso — `permissoesPessoasMesmoPerfil` só olhava o campo `perfil` puro, sem diferenciar os 3 níveis (comum/naipe/admin) que passaram a existir dentro de "diretor". Ela corrigiu manualmente os afetados na hora, sob pressão. Corrigido com `cargoChaveDiretoria(p)`, mesma prioridade "admin manda mais" já usada em Permissões, aplicada tanto no agrupamento quanto no rótulo mostrado.

✅ **Permissões Padrão: Convidados Simples/Especial separados e aplicados sozinhos por bateria (04/set/2026, commit `05d1ba2`).** Ela percebeu que o molde de "Diretor Admin" tinha ligado capacidades de Convidado Especial numa bateria que usa Convidado Simples — a tela de Permissões Padrão é global (não por bateria) e mostrava os dois modelos misturados sob o mesmo título "Convidados". Rótulo separado em "(modelo Simples)"/"(modelo Especial)"; e o backend ganhou `jsonb_capacidades_padrao_filtrado(molde, bateria_id)`, usado nas 3 triggers de aplicação automática — filtra sozinho, por bateria, só as capacidades do modelo que ela realmente usa, antes de aplicar/mesclar o molde. Limpeza pontual: Erick e Thaís (Rocinha/Ritmo Avassalador, Convidado Simples) tiveram as 5 chaves dormentes de Convidado Especial removidas. Ela sinalizou cansaço no meio da sessão ("eu não posso ficar maluca com isso") — combinado retomar com calma, em sessão futura, se vale simplificar a convivência dos dois modelos de Convidado (ver `project_convidado_simples_vs_especial_incerteza` na memória).

✅ **Permissões Padrão por cargo + "Admin da Bateria" — publicado em produção (04/set/2026, commit `f2cf953`).** Pedido dela: "nunca mais terei trabalho de lembrar disso, que tenho que colocar permissão pra pessoas assim que elas caem no cadastro." Super Admin → Configurações → **Permissões Padrão**: 4 moldes de capacidades (Mestre/Diretor Admin/Diretor de Bateria/Diretor Apoio), globais (não por bateria) — tabela nova `permissoes_padrao`. Uma trigger nova (`trg_permissoes_padrao_vinculos`) aplica o molde certo automaticamente na 1ª aprovação de cada pessoa, em qualquer bateria (só se `capacidades` ainda estiver vazio — nunca sobrescreve quem já foi configurado na mão). **"Admin da Bateria"** virou um atributo de verdade do Diretor (coluna `vinculos.eh_admin_bateria`, capacidades próprias `ver_admin_bateria`/`editar_admin_bateria`) — decidido **na ficha** (não em Permissões), mesmo padrão já usado por "Naipe que lidera": o app já separava "o que a pessoa É" (ficha) de "a consequência de acesso disso" (Permissões, ex: Restrito ao Naipe) — Admin seguiu essa mesma lógica depois de ela questionar o desenho inicial (rascunho 1: instant-click solto na ficha, rejeitado por aparecer dentro de "Contato de Emergência" sem sentido e não passar por edição de verdade; rascunho 2: dentro do editor de Permissões, ela mesma reconsiderou comparando com Naipe). Cria 4º grupo em Permissões, "Diretor Admin" (mesmo padrão de "Diretor de Bateria - Naipe" — agrupamento automático). **"Admin manda mais"**: quem é Admin E tem Naipe (caso real, Imperatriz) aparece só no grupo Admin — o Naipe continua salvo/editável normalmente. Ordem final, pedido dela ("muito importante de visualizar"): Admin sempre antes de Naipe, tanto na ficha quanto em Permissões. Carteirinha nunca muda (cargo impresso continua normal) — selo "Admin" é só interno. **Achado no caminho, mesma sessão**: campo "Título do relatório" do Exportar nunca aparecia desde que foi criado (24/ago) — usava a classe `fc-input`, que tem `display:none` por padrão (pensada só pro campo de edição da ficha); corrigido com `display:block` explícito.

✅ **Corrigida falha silenciosa no upload de foto (cadastro e Meu Perfil) — publicado em produção (04/set/2026, commit `aaa2b78`).** Achado via suporte real (Luiz Henrique, "tentando há dias e não consigo") — sem tratamento de erro, uma foto que o navegador não conseguisse ler (ex: HEIC de iPhone, arquivo corrompido) falhava em silêncio, sem avisar nada; a pessoa ficava presa sem entender o motivo. Conferido no banco: ele não tinha nenhum cadastro nem conta criada, confirmando que travava antes do primeiro passo. Adicionado `reader.onerror`/`img.onerror` em `previewFoto` (cadastro.html) e `fpPreviewFoto` (ficha-perfil.js, Meu Perfil/Admin editando foto de outra pessoa) — agora mostra mensagem de verdade.

✅ **Convidado Simples unificado com Convidado Especial — publicado em produção (04/set/2026, commit `5e9f3a7`).** Decisão dela: acabar com o modelo antigo (tabela `extras`) — motivo: "não quero ter que ficar perguntando cpf, celular, dados de endereço, nada" pra um convidado que só vai receber uma camisa. O interruptor por bateria (`baterias.convidado_tem_carteirinha`) continua exatamente onde estava — ela foi enfática: "Não!!!! Não seria por pessoa, seria por bateria mesmo" — só que agora os dois lados usam a MESMA estrutura (`vinculos`, `eh_convidado=true`): "com carteirinha" continua com formulário completo + autocadastro por link; "sem carteirinha" vira cadastro manual (nova capacidade `criar_cadastro_convidados_especiais`, já no molde de Mestre/Diretor Admin), só Nome + Tamanho de Figurino obrigatórios, e-mail/senha de login gerados sozinhos (essa pessoa nunca acessa o app). Botão "+ Cadastrar Convidado" existe nos DOIS modelos (correção de rumo dela: "eu quero que tenha isso nos dois modelos", mesmo padrão de Ritmistas/Diretoria — botão de cadastro manual e link juntos). Painel antigo de Convidados (`extras`) fica congelado no código — nenhuma bateria real tinha dado lá quando isso foi decidido (só 2 baterias de teste, 2 linhas cada). Detalhe técnico completo e itens ainda em aberto (apagar tabela `extras` de vez, renomear rótulo "Convidados (modelo Especial)"): memória `project_unificacao_convidado_especial_desenho`.

✅ **Tela antiga do Convidado Simples removida do código + Figurino/Presença corrigidos (04/set/2026, mesma sessão, commit `dfb2447`).** Pedido dela ("pode apagar a tela do convidado simples") revelou um problema maior: a unificação acima só tinha trocado a TELA — Entrega de Figurino e Lista de Presença ainda buscavam Convidado na tabela antiga (`extras`) pra bateria sem carteirinha, então um Convidado novo sumiria dessas duas telas. Corrigido: `modoConvidadosEspecial()` (função interna) virou sempre `true` — todo o roteamento de dados (Figurino/Presença/gate de aba) passa a usar sempre `vinculos`/`eh_convidado`. **Dois erros reais cometidos e corrigidos na mesma sessão, registrados pra não repetir:** (1) 1ª tentativa de apagar a tela removeu um intervalo contíguo de linhas sem perceber que o módulo real de Convidado Especial (aprovar/rejeitar/suspender) estava fisicamente intercalado no meio do código antigo — quebrou a tela inteira (só no link de preview, nunca publicado); corrigido revertendo e refazendo função por função, com grep de confirmação antes/depois de cada remoção. (2) Ao forçar `modoConvidadosEspecial()`, o link de autocadastro passou a aparecer também pra bateria SEM carteirinha (achado dela ao vivo, testando Jacarezinho) — a função tinha 2 significados diferentes conflitados; `renderizarLinksCadastroConvidadoEspecial()` corrigida pra usar `bateriaAtualData.convidado_tem_carteirinha` direto nessa decisão específica.

✅ **Marcar presença/entrega travado até o evento/entrega ser "Iniciado" — publicado em produção (04/set/2026, commit `3e0e9ef`).** Achado dela ao vivo, com print: os interruptores "Evento Iniciado"/"Entrega Iniciada" já existiam nas telas de Presença/Figurino, mas o botão de marcar presença/entrega funcionava mesmo com eles desligados — só dependia da permissão da pessoa, nunca do estado do evento/entrega. Corrigido: o botão só fica ativo com Iniciado=true e Finalizado=false (mesmo critério de "evento ativo" já usado na Visão Geral); sem isso vira selo sem ação, mesmo tratamento visual já usado quando falta permissão. Trava replicada também por dentro (não só no botão), cobrindo o caso raro de alguém já ter aberto a confirmação antes do interruptor mudar. Interruptores agora atualizam a lista na hora, sem sair/voltar da tela.

🚧 **Outras pendências abertas, sem urgência definida:**
- **Lógica de Temporada** (recadastro leve por temporada + trava de carteirinha/painel pra quem não revisou) — desenho evoluiu bastante desde 27/ago/2026: cascata hierárquica de renovação (Mestre/Adm → Diretoria → Ritmistas → Convidados), status novo "Não Renovado" (não vira Desligado sozinho), retorno de Desligado/Rejeitado (reaproveita cadastro existente pra quem já tem vínculo em outra bateria; revisão completa pré-preenchida, ainda não implementada, pra quem não tem vínculo nenhum), Declaração do Responsável (menores) volta a ficar pendente na renovação (02/set/2026), nova aba "Temporadas" (fotografia agregada por temporada + relatório de faltosos em Presença por evento, detalhe de nome se apaga na virada de temporada — só ela, nenhum outro domínio como Figurino). **Nada implementado ainda** — desenho completo fechado com ela em 02/set/2026, registrado com cuidado por ela ter pedido explicitamente ("esse assunto é bem delicado"). Ver `docs/tumtu-historico-sessoes.md` (última entrada) antes de retomar.
- **"Ritmistas/Diretoria sem carteirinha esta temporada"** (bulk import com senha aleatória, pra Mestre que já pagou carteirinha física) — "Fica pra depois", 28/ago/2026.
- **Área dedicada "Configurações → Carteirinha"** — hoje `validade_carteirinha` mora provisoriamente dentro do cadastro de escola.
- 🟡 **Faixa branca do notch — reduzida bastante, não eliminada 100%; pendência do canto do spinner, sem urgência.** Histórico completo em `docs/tumtu-historico-sessoes.md` (sessões de 03/set, 05-06/set e 06/set continuação). Estado atual, resumido:
  - **`viewport-fit=cover` continua proibido** sem reconstruir a barra fixa de baixo primeiro (quebrou 2x quando testado) — não religar sem ferramenta de teste real num iPhone físico.
  - **A causa raiz real do "corte" na transição login→admin era JS, não CSS**: um bloco de 526KB de `<script>` inline sem `defer` no meio de `admin.html` bloqueava a pintura da página. **Corrigido e publicado (06/set continuação)** — extraído pra `admin-logic-1.js`/`admin-logic-2.js`, carregados com `defer`.
  - **Experimento adicional publicado**: transição nativa entre páginas (View Transition API, suportada desde Safari 18.2) religada só pra entrada login→admin (nunca na saída, trava por `pageswap`/`skipTransition()`). Resultado dela: "melhorou muito. mas ainda continua [uma piscadinha]" — mantido no ar, net-positive mas não solução completa.
  - **Pendência aberta, sem urgência**: tentativa de reduzir a moldura clara do topo do spinner (deixar o cartão preto mais alto) falhou de novo ("ultrapassou tudo") — causa entendida (o border-radius da moldura, 44px, é maior que o do cartão, 30px; com padding zero sobra uma "meia-lua" clara nos cantos), mas sem solução implementada. Revertido pro padding de 20px nos 4 lados (estado seguro, sem problema há meses). Ela pediu explicitamente pra não retomar sem ela chamar — **não insistir nisso de novo por conta própria**.
  - ✅ **Botão "Super Admin" sumido no celular — causa real achada e CONFIRMADA por ela (06/set continuação)**: `.nav-abas-voltar` (o botão flutuante) mora dentro de `<nav id="navAbasEscola">`, que herda `overflow-y:auto` da regra de computador — bug conhecido do Safari corta um elemento `fixed` filho de um ancestral `fixed`+`overflow`. Corrigido com `overflow:visible` na regra mobile. "testei, o botão do super admin apareceu." Lentidão de clique (mesmo sintoma de 038cde3) corrigida junto, nos dois botões (Super Admin e Trocar de Bateria).
✅ **Badge de pendentes no menu (Ritmistas/Diretoria/Convidados) — publicado em produção (03/set/2026, commit `e35be38`, merge na `main`).** Testado por ela na bateria Jacarezinho, funcionando certinho. Selo no ícone do app (fora do navegador, Badging API) não dá pra testar via link de preview — só verificável de verdade com o app instalado a partir do domínio real; suporte no iPhone continua fraco/inconsistente (aceito por ela conscientemente).
- **"Área de Comunicação"** (nome ainda não decidido) — desenho detalhado fechado com ela em 02/set/2026: mensagens da bateria (todo mundo/grupo por instrumento/pessoa só), aniversariantes do dia + enviar parabéns (mensagem direcionada, não é mural público), vitrine de acessórios à venda (nunca compra de verdade — só vitrine + código Pix como instrução, estoque por tamanho reaproveitando o padrão do Figurino, fluxo de reserva em 2 etapas com anexo de comprovante via Supabase Storage, apagado ao decidir), aviso de mensagem nova (dentro do app sempre funciona + selo no ícone melhor esforço). **Nada implementado ainda**. Ver `docs/tumtu-historico-sessoes.md` (última entrada).
- **Analytics gratuito vinculável ao Dashboard** — Vercel Web Analytics (grátis, sem cookie). Script já colocado em todas as páginas (commit `40511d4`, 02/set/2026) — **ainda não conta nada de verdade**, falta ligar a coleta no painel da própria Vercel (Project Settings → Analytics, passo manual fora do alcance desta sessão). Depois disso, falta ainda o card dentro do Dashboard do TumTu puxando os números pela API pública. Ver `docs/tumtu-historico-sessoes.md`.
- **Textos de ajuda (`config-sub-desc`) do resto do app** — todo o Super Admin (Dashboard, Escolas, Configurações + 5 subtelas, Privacidade) já foi revisado com a regra de estilo nova (02-03/set/2026, ver `feedback_regra_legenda_titulo_objetiva` na memória). **Falta ainda o resto**: telas por bateria (Instrumentos/Vagas/Figurino/Eventos/Entrega de Figurino/Lista de Presença/Permissões/Histórico dentro de uma escola específica) — ela não pediu essa extensão ainda, mas a régua já existe pronta pra reaproveitar quando ela quiser.
- Ideias soltas, sem decisão/desenho ainda (ver memória de longo prazo pra detalhe de cada uma): mascarar dados sensíveis em Meu Perfil (toque-pra-revelar), suporte via WhatsApp, suspender acesso por inadimplência, preview de cor de carteirinha sem criar escola demo.

📦 **Backlog "só se o TumTu crescer muito"** (rebaixado por decisão explícita dela, 17/jul/2026 — não sugerir de novo sem o cenário mudar): "Leaked Password Protection" (depende de plano pago do Supabase).

🚫 **Não retomar sem ela pedir de novo**: carteirinha offline (decisão fechada 17/jul/2026); personalização de layout arrastável da Visão Geral (duas tentativas revertidas em 27/ago/2026 — só retomar com ferramenta de teste de navegador disponível).

---

## Contas de teste

Banco roda só com dados fake (`dados/tumtu-dados-fake-reset.xlsx`). Senha padrão de todas as contas fake: `Teste123`. Super Admin (Márcia): e-mail `tumtuapp@gmail.com`, senha `tumtu2027`.
